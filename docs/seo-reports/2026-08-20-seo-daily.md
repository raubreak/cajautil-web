# Reporte SEO diario de CajaUtil

**Fecha de ejecucion:** 2026-08-20  
**Propiedad GSC:** `sc-domain:cajautil.com`  
**Estado:** completado; mejoras tecnicas posteriores documentadas al final

## Resumen ejecutivo

- GSC registro 214 impresiones y 0 clics entre el 23 de julio y el 19 de agosto, frente a 463 impresiones y 0 clics en los 28 dias anteriores. La visibilidad bajo un 53,8%.
- La caida se concentra en `/calculadora-sueldo-neto`: 193 impresiones frente a 428 (-54,9%). Su posicion media mejoro de 82,3 a 77,9, por lo que el problema observable no es una perdida de posicion ni un snippet con CTR anormal, sino una visibilidad todavia muy lejana de la primera pagina.
- GA4 registro 2 sesiones organicas en los ultimos 28 dias, ambas en `/mayusculas-minusculas`. En los ultimos 7 dias completos registro 0, frente a 2 en los 7 dias anteriores.
- No aparecieron consultas emergentes con al menos 3 impresiones y crecimiento semanal, ni combinaciones pagina-consulta con al menos 10 impresiones, CTR inferior al 5% y posicion media entre 3 y 20.
- No se aplicaron cambios on-page: los datos no permiten atribuir el CTR del 0% a metadata cuando las principales posiciones medias estan entre 65 y 95.
- No se genero borrador editorial: no existe una consulta con demanda y una intencion diferenciada suficientemente demostradas.

## Periodos analizados

| Comparacion | Periodo actual | Periodo anterior |
|---|---|---|
| GSC, 28 dias completos | 2026-07-23 a 2026-08-19 | 2026-06-25 a 2026-07-22 |
| GSC, 7 dias completos | 2026-08-13 a 2026-08-19 | 2026-08-06 a 2026-08-12 |
| GA4, 28 dias completos | `28daysAgo` a `1daysAgo` | `56daysAgo` a `29daysAgo` |
| GA4, 7 dias completos | `7daysAgo` a `1daysAgo` | `14daysAgo` a `8daysAgo` |

Los datos se consultaron mediante las integraciones configuradas del repositorio. No se leyeron ni mostraron archivos de secretos.

## Google Analytics 4

### Trafico organico

| Periodo | Sesiones | Vistas | Sesiones con interaccion |
|---|---:|---:|---:|
| Ultimos 28 dias completos | 2 | 2 | 2 |
| 28 dias anteriores | 0 | 0 | 0 |
| Ultimos 7 dias completos | 0 | 0 | 0 |
| 7 dias anteriores | 2 | 2 | 2 |

La unica landing organica registrada en la ventana de 28 dias fue `/mayusculas-minusculas`, con 2 sesiones, 2 vistas y 2 sesiones con interaccion. El volumen es demasiado bajo para inferir tendencias de comportamiento o conversion.

## Google Search Console

### Rendimiento agregado

| Periodo | Clics | Impresiones | CTR |
|---|---:|---:|---:|
| Ultimos 28 dias completos | 0 | 214 | 0% |
| 28 dias anteriores | 0 | 463 | 0% |
| Ultimos 7 dias completos | 0 | 3 | 0% |
| 7 dias anteriores | 0 | 23 | 0% |

La comparacion semanal cae de 23 a 3 impresiones (-87,0%), pero el volumen absoluto es bajo. La serie diaria muestra picos intermitentes, especialmente entre el 6 y el 22 de julio, seguidos de una fase de baja exposicion. No hay clics perdidos ni evidencia de una caida desde posiciones competitivas.

### Paginas principales

| URL | Impresiones | Anteriores | Variacion | CTR | Posicion | Posicion anterior |
|---|---:|---:|---:|---:|---:|---:|
| `/calculadora-sueldo-neto` | 193 | 428 | -54,9% | 0% | 77,9 | 82,3 |
| `/lector-qr` | 11 | 27 | -59,3% | 0% | 65,0 | 65,6 |
| `/` | 5 | 7 | -28,6% | 0% | 23,6 | 60,1 |
| `/articulos/interes-compuesto-la-diferencia-entre-aportar-pronto-y-aportar-tarde` | 2 | 1 | +100% | 0% | 8,0 | 8,0 |
| `/generador-qr` | 2 | 0 | nueva | 0% | 3,0 | sin datos |

Las dos ultimas URLs entran en posiciones potencialmente utiles, pero solo suman 2 impresiones cada una. Se mantienen en observacion; editar ahora seria reaccionar a una muestra insuficiente.

### Consultas con mas impresiones

| Consulta | Impresiones | Anteriores | CTR | Posicion |
|---|---:|---:|---:|---:|
| `calcular sueldo` | 7 | 5 | 0% | 94,6 |
| `calculadora sueldo` | 6 | 9 | 0% | 85,7 |
| `calculadora sueldo neto` | 6 | 25 | 0% | 82,3 |
| `calculadora salario` | 5 | 3 | 0% | 84,2 |
| `18.000 brutos a netos 12 pagas` | 4 | 1 | 0% | 68,5 |
| `calculadora de salario` | 4 | 3 | 0% | 83,3 |
| `calculadora salarial` | 4 | 1 | 0% | 86,8 |

La consulta mas cercana a una oportunidad editorial especifica, `18.000 brutos a netos 12 pagas`, solo aporta 4 impresiones y posicion 68,5. Ademas, su intencion ya esta cubierta por la calculadora salarial y sus tablas; crear otra URL aumentaria el riesgo de canibalizacion.

## Diagnostico

### Hallazgo 1: descenso de impresiones sin perdida equivalente de posicion

- **Categoria:** visibilidad organica.
- **Evidencia:** 214 impresiones frente a 463; `/calculadora-sueldo-neto` pasa de 428 a 193 mientras mejora 4,4 posiciones de media.
- **Severidad:** media.
- **Confianza:** alta en la medicion; baja para atribuir una causa concreta.
- **Interpretacion:** no se observa un incidente de CTR ni una regresion tecnica demostrada. La exposicion de la URL sigue ocurriendo principalmente en resultados muy profundos.
- **Accion:** mantener seguimiento durante al menos otro ciclo completo de 28 dias y separar cualquier efecto de los cambios desplegados el 20 de agosto.

### Hallazgo 2: volumen insuficiente para optimizar snippets

- **Categoria:** on-page.
- **Evidencia:** ninguna combinacion pagina-consulta supera simultaneamente 10 impresiones y posicion media de 3 a 20; las consultas principales estan en posiciones 68 a 95.
- **Severidad:** baja.
- **Confianza:** alta.
- **Interpretacion:** el CTR del 0% es esperable en esas posiciones y no demuestra que los titulos o descripciones fallen.
- **Accion:** no modificar metadata hasta reunir una muestra competitiva.

### Hallazgo 3: senales tempranas sin volumen

- **Categoria:** crecimiento.
- **Evidencia:** `/generador-qr` aparece 2 veces en posicion media 3 y el articulo de interes compuesto 2 veces en posicion 8.
- **Severidad:** oportunidad en observacion.
- **Confianza:** baja por tamano muestral.
- **Accion:** revisar de nuevo si cualquiera alcanza 20 impresiones en posiciones 3 a 15 o genera clics.

## Comprobaciones on-page y tecnicas

- `https://cajautil.com/calculadora-sueldo-neto` responde `200`, permite `index, follow` y declara canonical autorreferente correcto.
- `https://cajautil.com/lector-qr` responde `200`, permite `index, follow` y declara canonical autorreferente correcto.
- La metadata de la calculadora salarial ya describe bruto, neto, IRPF, cotizacion y 12/14 pagas, alineada con las consultas observadas.
- La calculadora salarial recibio el 20 de agosto una correccion desplegada de estimaciones y descripciones. No se superpone otra modificacion para preservar una ventana de medicion interpretable.
- No se detecta evidencia suficiente para reactivar generacion programatica, escribir en Prisma, publicar contenido o modificar sitemap.

## Cambios aplicados

- Ningun cambio SEO on-page.
- Ningun borrador editorial.
- Se creo exclusivamente este reporte diario.

## Validaciones ejecutadas

- `npx tsx scripts/ga4-audit.ts`: correcto; API accesible.
- Consulta ampliada de GA4 por landing y comparativas de 7/28 dias: correcta.
- Verificacion de acceso GSC y consultas por pagina, consulta, pagina-consulta y fecha: correcta.
- `npm run editorial:audit`: correcto; 9 articulos auditados y ninguno por debajo del minimo interno de 250 palabras.
- Comprobacion HTTP y metadata de las principales URLs afectadas: correcta.

No se ejecutaron lint, TypeScript ni build porque no se modifico codigo. No se hizo commit ni push.

## Proximo control

- Vigilar si `/generador-qr` o el articulo de interes compuesto acumulan al menos 20 impresiones en posiciones 3 a 15.
- Revisar `/calculadora-sueldo-neto` tras un ciclo de 28 dias posterior al cambio del 20 de agosto; no atribuir mejoras o caidas antes de disponer de esa ventana.
- Investigar indexacion o calidad solo si la calculadora salarial deja de recibir impresiones de forma sostenida o pierde posicion de forma clara; hoy sigue indexable y su posicion media ha mejorado.
- Mantener bloqueada la creacion editorial mientras no exista una consulta con demanda demostrable e intencion no cubierta.

## Actualizacion manual posterior

Tras el reporte diario se reviso una ventana ampliada de GSC. `/calculadora-sueldo-neto` concentra 665 impresiones y posicion media 86; `/lector-qr` suma 36 impresiones y posicion 69,3. No aparecio una intencion nueva que justificara otra URL sin riesgo de canibalizacion.

Se reforzo la guia salarial existente en lugar de publicar variantes por importes:

- La guia paso de 752 a 1.094 palabras e incorpora una tabla reutilizable para comparar ofertas.
- Se añadieron referencias verificadas a las retenciones 2026 de la AEAT y a las bases y tipos de cotizacion 2026 de la Seguridad Social.
- Se añadieron enlaces contextuales hacia la calculadora salarial y las guias de vivienda e interes compuesto.
- Las referencias genericas de la calculadora se sustituyeron por las paginas oficiales exactas.
- Commit publicado: `70d23f1 feat(content): deepen salary decision guide`.

Validacion posterior al despliegue:

- Vercel completo el despliegue correctamente.
- Las 42 URLs del sitemap responden `200`, permiten indexacion y no presentan duplicados de titulo o descripcion.
- La pagina publica muestra la tabla, las fuentes oficiales y los enlaces internos sin errores de consola.
- El sitemap expone `2026-08-20T17:18:00.000Z` como `lastmod` de la guia salarial.
- El sitemap se reenvio a GSC y la API respondio `204`.
- La publicidad de terceros permanece desactivada; el aviso de consentimiento confirma que solo se solicita permiso para analitica.

### Actualizacion del cluster QR

Se corrigio una incoherencia entre la herramienta y sus recomendaciones editoriales:

- Las descargas seleccionadas por encima de 200 px usaban un lienzo mayor, pero el QR conservaba el tamaño de la vista previa en una esquina. Ahora el SVG se escala al tamaño completo del PNG.
- La descarga incorpora la zona de silencio de cuatro modulos indicada por DENSO WAVE y mantiene correccion de errores de nivel H.
- La guia QR paso de 524 a 979 palabras con referencias sobre margen, tamaño de modulo y correccion de errores, ademas de un preflight para impresion.
- La descripcion del generador ya no promete formatos estructurados de WiFi que la interfaz no construye.
- Commit publicado: `3c853af fix(qr): export reliable printable codes`.

La prueba funcional en produccion genero un PNG de 600 x 600 px, con el patron distribuido entre los pixeles 65 y 534 y margen limpio alrededor. La API `BarcodeDetector` de Chrome recupero correctamente `https://cajautil.com`. El rastreo mantuvo 42 URLs indexables sin incidencias y el sitemap se reenvio a GSC con respuesta `204`.

### Actualizacion de fuentes YMYL

Se reforzaron las guias y herramientas de prestamos, IVA e IBAN con fuentes oficiales que respaldan cada afirmacion relevante:

- Prestamos enlaza las paginas vigentes del Banco de Espana sobre prestamos personales y calculo de la TAE, y separa TIN, TAE, cuota, total devuelto y vinculaciones.
- IVA enlaza el calculo del impuesto repercutido y los tipos impositivos de la AEAT; tambien distingue la operacion aritmetica de la clasificacion fiscal.
- IBAN incorpora la definicion oficial de identificador unico y el servicio gratuito de verificacion del beneficiario vigente desde el 9 de octubre de 2025.
- Las referencias genericas de las tres herramientas se sustituyeron por las paginas oficiales exactas.

Validacion previa a publicacion: ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y build de produccion correctos. La auditoria registro 10 articulos, ninguno por debajo de 250 palabras y ninguna fecha de actualizacion duplicada.

### Inspeccion de indexacion y segunda revision editorial

La API de inspeccion de URLs de Search Console mostro el estado conocido por Google para las 42 URLs del sitemap:

- 5 URLs enviadas e indexadas: portada, politica de privacidad, calculadora salarial, generador QR y lector QR.
- 2 articulos rastreados pero aun no indexados: IVA e interes compuesto.
- 11 URLs descubiertas pero sin indexar.
- 24 URLs que Google todavia no reconoce.

Las URLs indexadas permiten rastreo, se descargaron correctamente y usan la canonical declarada. El rastreo HTTP independiente sigue mostrando 42 respuestas `200`, 42 URLs indexables y ninguna duplicidad de titulo o descripcion. Por tanto, la evidencia apunta a seleccion de indice, autoridad y madurez del sitio, no a un bloqueo por robots o canonical.

No se reactivo la generacion pSEO. Los datos de GSC solo muestran demanda consistente para calculadora salarial y lector QR, ya cubiertas por sus URLs principales; crear permutaciones sin valor propio aumentaria contenido duplicado y paginas puerta.

Se reforzaron las tres guias editoriales mas cortas y antiguas:

- WebP paso de 543 a 811 palabras con fuentes de Google Developers y web.dev, una tabla de seleccion por tipo de imagen y una correccion de la afirmacion sobre SEO: el formato no posiciona por si solo.
- Contrasenas paso de 388 a 660 palabras y se alineo con NIST SP 800-63B sobre longitud, reglas de composicion, cambios periodicos, gestores y autenticacion resistente al phishing.
- IMC, calorias y TDEE paso de 344 a 601 palabras con fuentes de OMS, NHS y el estudio de Mifflin-St Jeor, diferenciando estimacion, seguimiento y diagnostico.

Monetag y otros scripts de popunder o barras intrusivas no aparecen en el codigo fuente. La publicidad de terceros continua desactivada y el consentimiento existente se limita a analitica opcional.

### Descubrimiento mediante IndexNow

Se incorporo una via adicional de descubrimiento para Bing y los motores participantes en IndexNow, sin alterar el sitemap ni enviar paginas `noindex`:

- La clave de propiedad se sirve desde la raiz de `cajautil.com` y su contenido se verifico en produccion.
- `scripts/submit-indexnow.ts` obtiene las URLs directamente del sitemap publico y rechaza hosts ajenos o lotes fuera del limite oficial.
- El primer lote incluyo las 42 URLs canonicas del sitemap y recibio `HTTP 202`: URLs aceptadas con validacion de clave pendiente.
- Esta respuesta confirma la recepcion, no la indexacion. Google no participa en IndexNow y sigue dependiendo del sitemap, el enlazado y su propio rastreo.
- Commit publicado: `84543c3 feat(seo): notify search engines with IndexNow`.

### Refuerzo del grafo editorial

El rastreo del HTML publico confirmo que no habia paginas huerfanas, pero cinco articulos solo recibian enlaces desde dos URLs internas. Se añadieron enlaces contextuales desde las guias de QR y compra de vivienda hacia IVA, sueldo neto, IBAN, contrasenas y WebP.

Tras el despliegue:

- Los 10 articulos reciben enlaces HTML desde al menos tres paginas distintas.
- Las cinco referencias nuevas aparecen en el HTML inicial y no dependen de una interaccion del usuario.
- Las 42 URLs del sitemap responden `200`, siguen siendo indexables y no presentan incidencias ni metadata duplicada.
- El sitemap refleja `2026-08-20T18:09:35.000Z` para la guia QR y `2026-08-20T18:09:36.000Z` para vivienda.
- IndexNow recibio solo esas dos URLs modificadas con `HTTP 200`; GSC acepto el reenvio del sitemap con `HTTP 204`.
- Commits publicados: `4a77cc4 feat(seo): strengthen editorial topic links` y `72965e9 fix(seo): submit only changed IndexNow URLs`.

### Coherencia y referencia publica en GitHub

El repositorio publico todavia describia CajaUtil como un MVP orientado a Monetag e impresiones publicitarias, mostraba herramientas ya publicadas como pendientes y enlazaba al alias antiguo de Vercel. Esa informacion contradecia la politica visible del sitio y debilitaba la confianza en el proyecto.

- El README ahora documenta las herramientas, privacidad, publicidad desactivada, validaciones y estructura reales.
- La pagina del repositorio enlaza directamente a `https://cajautil.com` y declara una descripcion coherente con el servicio actual.
- Se añadieron los temas `calculators`, `nextjs`, `privacy-first`, `spanish`, `typescript` y `web-tools` para mejorar descubrimiento dentro de GitHub.
- La pagina publica de GitHub se verifico sin autenticar y muestra correctamente el enlace canonico y el README.
- Commit publicado: `1114182 docs: align repository with CajaUtil`.

### Optimizacion de estabilidad visual y LCP movil

La primera correccion del buscador elimino el cambio de tamaño que producia el placeholder cargado de forma dinamica. Una medicion posterior al despliegue, en un contexto movil nuevo con Fast 4G y CPU 4x, confirmo `CLS 0`, pero descubrio que el aviso de cookies aparecia tarde y se convertia en el LCP a los 26,07 segundos.

Se corrigio la causa, no solo el sintoma:

- El aviso de cookies forma parte del HTML inicial en lugar de insertarse 800 ms despues de la hidratacion.
- Los usuarios con una decision guardada lo ocultan antes del primer render visible, sin destello ni warning de hidratacion.
- El texto visible se acorto sin retirar el enlace a la politica completa ni las opciones de aceptar y rechazar.
- El header monta una sola instancia del buscador en vez de duplicarla para escritorio y movil.

Validacion local con viewport de 390 x 844, Fast 4G y CPU 4x:

| Estado | LCP | CLS | Elemento LCP |
|---|---:|---:|---|
| Visitante nuevo | 1,49 s | 0 | Descripcion principal de la portada |
| Rechazo guardado | 3,70 s | 0 | Descripcion principal de la portada |

Las cifras son mediciones de laboratorio y no sustituyen los datos de campo. ESLint dirigido, TypeScript, build de produccion de 57 rutas, consola del navegador y flujo de gestion del consentimiento quedaron verificados.

Tras desplegar `f16bf1c`, una navegacion de produccion con almacenamiento limpio y la misma emulacion registro LCP de 8,10 s y CLS de 0,0001. La cifra absoluta conserva la sobrecarga y variabilidad del laboratorio, pero el elemento LCP ya es la descripcion principal y no el aviso de cookies tardio. El rastreo posterior confirmo 42 de 42 URLs con respuesta `200` e indexables, sin incidencias ni metadata duplicada.

### Consolidacion editorial dentro de herramientas

Una nueva comprobacion de GSC mantuvo el mismo diagnostico: `/calculadora-sueldo-neto` acumula 665 impresiones en posicion media 86 y `/lector-qr` 36 impresiones en posicion 69,3. No existe demanda diferenciada suficiente para publicar otra URL; ampliar el inventario ahora aumentaria el riesgo de canibalizacion y paginas no seleccionadas para el indice.

La auditoria del HTML detecto una mejora interna mas prioritaria. Varias herramientas mostraban una guia propia y, despues, otra guia global con una segunda FAQ y otro bloque de enlaces relacionados. La calculadora salarial llegaba a reunir contenido editorial en el componente interactivo, en la pagina y en el bloque global.

- Las herramientas con contenido propio conservan su guia y reciben del bloque global solo advertencias, revision editorial y referencias.
- Las herramientas sin guia propia mantienen el bloque global completo con contexto, FAQ y enlaces internos.
- Se retiro contenido SEO duplicado del componente interactivo de sueldo neto.
- Se elimino el `FAQPage` no visible de porcentajes y se mantuvo su `HowTo`.
- La identidad publica se unifico como Raul Santos y el rol se describe como desarrollo y responsabilidad editorial, sin atribuir especializacion financiera o sanitaria no demostrada.

La auditoria de las 42 URLs locales no encontro ninguna con mas de un `FAQPage`, encabezado FAQ, guia global o bloque de herramientas relacionadas. Las fuentes oficiales y los avisos YMYL permanecen visibles en las paginas compactadas.

Verificacion posterior al despliegue `4cd1b97`: las 42 URLs responden `200`, son indexables y no presentan incidencias ni metadata duplicada; la auditoria DOM de produccion tampoco encontro duplicaciones editoriales o JSON-LD invalido. IndexNow recibio las 19 herramientas indexables modificadas con `HTTP 200`. Las tres utilidades modificadas que permanecen en `noindex` se excluyeron correctamente del envio.

### Cobertura de imagen social

La auditoria completa de metadata encontro 28 URLs indexables sin `og:image`; 11 tampoco emitian `twitter:image`. La causa era la fusion superficial de metadata anidada en Next.js: cuando una ruta definia su propio objeto `openGraph` o `twitter`, sustituia el objeto heredado y perdia la imagen global.

Se incorporo la imagen social existente de CajaUtil en los objetos afectados. El control local posterior confirmo cobertura de `og:image` y `twitter:image` en las 42 URLs del sitemap, manteniendo titulo, descripcion, canonical y un unico H1 en todas ellas. Esta mejora no aumenta rankings por si sola, pero evita previsualizaciones incompletas y facilita una distribucion coherente cuando las paginas se comparten o enlazan.

Tras desplegar `a67b616`, la misma auditoria sobre produccion confirmo cobertura social completa y un unico H1 en las 42 URLs, sin errores de consola. El rastreo mantuvo 42 respuestas `200`, 42 URLs indexables, cero incidencias y cero metadata duplicada. IndexNow recibio las 28 URLs modificadas con `HTTP 200`.

### Contenido editorial sin coste de hidratacion

El bloque editorial global se resolvia desde un componente cliente que importaba las 34 entradas completas de `toolEditorialContent`. Ese catalogo generaba un chunk de 96.091 bytes sin comprimir y aproximadamente 30 KB transferidos, aunque cada visita solo necesitaba la entrada de una herramienta.

Se movio la renderizacion a los layouts de las 34 herramientas. `ToolEditorialSection` queda como componente servidor y el router cliente global desaparece. El contenido sigue formando parte del HTML inicial, pero ya no se descarga ni se hidrata como JavaScript en el navegador.

Medicion de la calculadora salarial bajo el mismo perfil movil:

| Estado | Scripts | JavaScript codificado |
|---|---:|---:|
| Produccion anterior | 10 | 183.411 bytes |
| Build optimizado local | 9 | 154.947 bytes |

La reduccion observada es de 28.464 bytes, un 15,5%. La auditoria posterior confirmo las 34 secciones editoriales en HTML de servidor y cero incidencias de duplicacion o JSON-LD en las 42 URLs.

Tras desplegar `d762acf`, produccion sirvio 9 scripts y 159.031 bytes codificados en la calculadora salarial, un 13,3% menos que los 183.411 bytes anteriores. El chunk editorial dejo de cargarse y la consola permanecio limpia. El rastreo final confirmo 42/42 URLs del sitemap con respuesta `200`, canonical propio, indexabilidad, un unico H1, imagenes sociales y JSON-LD valido; las 34 herramientas conservaron su variante editorial correcta en el HTML inicial. IndexNow recibio las 25 herramientas indexables afectadas con `HTTP 200`.

## Calidad verificable y medicion de uso

La nueva consulta de datos no justifica ampliar el inventario editorial. GSC mantiene 665 impresiones para `/calculadora-sueldo-neto`, 36 para `/lector-qr`, 12 para la portada y 3 para `/generador-qr`, todas todavia lejos de posiciones competitivas. GA4 registra 0 sesiones organicas en los ultimos siete dias completos frente a 2 en la semana anterior. Publicar nuevas variantes o articulos sobre las mismas intenciones aumentaria la canibalizacion sin resolver la falta de autoridad.

Se priorizaron mejoras en las URLs existentes:

- La calculadora de edad rechaza fechas futuras, contempla el 29 de febrero y diferencia el calculo por calendario de las estimaciones por tiempo transcurrido.
- Hipotecas e interes compuesto explican el modelo constante, los costes no incluidos y la ausencia de garantias sobre resultados futuros.
- El generador de contrasenas garantiza al menos un caracter de cada grupo seleccionado y alinea sus recomendaciones con longitud, unicidad y aleatoriedad.
- El generador de hashtags deja claro que usa categorias locales y no datos de tendencias en tiempo real; permanece en `noindex,follow`.
- La metodologia publica incorpora un proceso de correccion y la autoria describe responsabilidades verificables sin atribuir credenciales financieras o sanitarias.
- Se eliminaron enlaces internos duplicados y se sustituyo la referencia hipotecaria generica por paginas concretas del Banco de Espana.

Tambien se incorporaron eventos `tool_started`, `tool_completed` y `result_copied` en sueldo neto y lector QR. Solo se envian tras aceptar analitica y contienen el slug de la herramienta; no incluyen salarios, porcentajes, archivos, nombres, contenido QR ni otros valores introducidos. `scripts/ga4-audit.ts` permite revisar estos eventos por ruta durante los ultimos 28 dias. La prueba local confirmo cero eventos sin consentimiento y los dos eventos esperados al completar una simulacion con consentimiento.

La busqueda en el codigo desplegable sigue sin encontrar Monetag, popunders ni redes publicitarias. `.vercelignore` excluye ahora `.atl/` y `.claude/` para impedir que worktrees o artefactos locales antiguos entren en un empaquetado manual de Vercel.

Tras desplegar `a92d2cc`, `2c4f86a` y `ef7fbbb`, la auditoria de produccion confirmo 42/42 URLs del sitemap con respuesta `200`, canonical propio, indexabilidad, un unico H1, imagenes sociales y JSON-LD valido. Las cinco paginas con afirmaciones corregidas ya no muestran el texto obsoleto, la politica de correcciones esta visible y no aparecen marcadores de publicidad intrusiva. Cincuenta contrasenas consecutivas de 16 caracteres incluyeron minuscula, mayuscula, numero y simbolo, sin errores de consola.

El envio selectivo a IndexNow revelo un caso limite: la portada aparece en el sitemap sin barra final, mientras `new URL('/')` la normaliza con barra. `scripts/submit-indexnow.ts` conserva ahora la forma canonica exacta publicada en el sitemap. La validacion posterior preparo y notifico 20 URLs indexables afectadas con `HTTP 200`.

## Grafo editorial contextual

Los diez articulos ya recibian enlaces desde herramientas, pero solo contenian siete enlaces contextuales entre guias y la distribucion era irregular. Se incorporo una seleccion manual de lecturas relacionadas:

- Nueve articulos muestran uno o dos enlaces a piezas con continuidad tematica real.
- La guia de IMC, calorias y TDEE queda sin recomendaciones porque no existe otra pieza editorial suficientemente proxima.
- El modulo usa titulos y descripciones especificas, forma parte del HTML inicial y mantiene una jerarquia H2/H3 valida.

El resultado local suma 17 enlaces editoriales visibles, sin referencias inexistentes, duplicadas ni autorreferentes. La auditoria automatizada ahora bloquea relaciones invalidas y tambien comprueba que cada articulo conserve al menos una fuente externa y un enlace contextual a su herramienta principal. Los diez articulos superan esas validaciones; los diez tienen revision posterior a publicacion y ninguno queda por debajo del objetivo editorial actual.

Tras desplegar `428b9f6`, la comprobacion de produccion confirmo los diez articulos con respuesta `200`, canonical propio, un unico H1 y JSON-LD valido. Las nueve piezas con continuidad tematica muestran sus lecturas relacionadas en el HTML inicial, con 17 enlaces en total; la guia de IMC, calorias y TDEE conserva deliberadamente cero recomendaciones. La consola del navegador no mostro errores, avisos ni incidencias. IndexNow recibio las diez URLs editoriales con `HTTP 200`.

## Feed editorial y nuevo control de descubrimiento

La inspeccion URL de GSC posterior a las mejoras mantiene cinco URLs indexadas y dos rastreadas sin indexar, pero muestra avance en descubrimiento: 16 URLs aparecen ahora como descubiertas sin indexar y 19 siguen sin ser reconocidas, frente a 11 y 24 respectivamente en el control anterior. El informe agregado del sitemap muestra temporalmente cero indexadas pese a que la inspeccion individual confirma cinco; se conserva esta diferencia como retraso o discrepancia entre informes y no como una desindexacion demostrada. El sitemap esta procesado, sin errores ni advertencias.

GSC continua sin mostrar una intencion nueva que justifique otra pagina: sueldo neto acumula 665 impresiones en posicion media 86, lector QR 36 en posicion 69,3, la portada 12 en posicion 58 y generador QR 3 en posicion 83. GA4 registra cero sesiones organicas en los ultimos siete dias completos y todavia no recibe los nuevos eventos de herramientas. Por tanto, publicar articulos adicionales ahora seguiria sin apoyo de demanda y aumentaria el riesgo de canibalizacion.

Se incorporo una via de seguimiento y descubrimiento que reutiliza el contenido editorial existente:

- `/feed.xml` publica las diez guias canonicas como RSS 2.0, con fechas, descripciones, GUID permanentes y enlace `self` canonico.
- El feed se genera como recurso estatico, escapa caracteres XML y se anuncia en el `head` global.
- `/articulos` ofrece un enlace visible de suscripcion y describe las diez guias mediante `CollectionPage` e `ItemList` coherentes con el contenido visible.

Tras desplegar `238ba74`, produccion devolvio el feed con `HTTP 200` y `application/rss+xml`, diez entradas y cero ampersands sin escapar. La autodeteccion, el enlace visible y los diez elementos del JSON-LD quedaron verificados sin errores de consola. IndexNow recibio `/articulos` con `HTTP 200` y GSC acepto de nuevo el sitemap con `HTTP 204`.

El validador de feeds de W3C confirmo que el RSS es valido y señalo una recomendacion de interoperabilidad por publicar `pubDate` y `dc:date` a la vez. El commit `22b6665` deja una unica fecha RSS basada en la ultima revision editorial; la respuesta publica actual contiene diez `pubDate` y ningun elemento ni namespace `dc:date`.

La canonicalizacion de protocolo funciona con redireccion permanente `308`, pero `https://www.cajautil.com` continua respondiendo con un `307` de Vercel antes de alcanzar `src/proxy.ts`. La aplicacion ya contiene una redireccion `308`; cambiar la respuesta de la capa de dominio requiere una sesion autenticada en Vercel y la CLI disponible en este entorno esta desconectada. No se introdujo una segunda regla que no podria ejecutarse antes de ese salto.

## Calidad de la calculadora de dias

Una auditoria de profundidad del texto visible detecto que `/calculadora-dias` solo exponia 128 palabras utiles porque su guia editorial estaba en modo compacto. La revision funcional encontro ademas que la interfaz llamaba exactos a los resultados en meses y anos, aunque se calculaban como aproximaciones por duracion media.

- La diferencia en dias usa ahora fechas UTC de calendario para no depender de cambios horarios locales.
- Semanas se identifica como semanas completas; meses y anos se etiquetan como aproximados.
- La interfaz explica que el intervalo excluye el dia inicial y muestra el total inclusivo cuando deben contarse ambos extremos.
- Se retiro un bloque de enlaces poco relacionado y se publico la guia editorial completa ya existente, sin generar texto de relleno.

La prueba con `2024-02-28` y `2024-03-01` devolvio dos dias transcurridos y tres dias inclusivos, contemplando el ano bisiesto. Tras desplegar `854a2ff`, produccion mostro 467 palabras visibles, un H1, canonical correcto y JSON-LD valido, sin errores de consola. IndexNow recibio la URL con `HTTP 200`; el sitemap incorpora el `lastmod` real mediante `d64a181`.

## Precision del extractor de colores

La revision funcional de `/extractor-colores`, todavia desconocida para Google, detecto un desbordamiento en la agrupacion RGB. Los canales entre 250 y 255 se redondeaban a 260 antes de convertirse a hexadecimal, por lo que un blanco podia aparecer como `#050504`. Los pixeles transparentes tambien participaban en la paleta como negro y una imagen extremadamente alta podia crear un lienzo desproporcionado.

- Los canales agrupados quedan limitados a 255 y los pixeles con alfa inferior a 128 se ignoran.
- Tanto el ancho como el alto de procesamiento tienen un maximo de 500 px.
- Al cambiar o retirar la imagen se limpian la paleta anterior y el estado del cuentagotas.

Una imagen sintetica mitad transparente y mitad blanca produce ahora solo `#FFFFFF`; una imagen roja de 10 x 1.000 px se procesa en un lienzo de 5 x 500 px y devuelve `#FF0000`. Tras desplegar `7423829` y `73fd63d`, produccion confirmo el resultado blanco, canonical correcto, un H1, JSON-LD valido y el `lastmod` `2026-08-20T20:44:39.000Z`, sin errores de consola. IndexNow recibio la URL con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Seguridad numerica de la regla de tres

La revision de `/calculadora-regla-de-tres`, tambien desconocida para Google, encontro que valores no finitos como `1e309` o una multiplicacion desbordada podian producir `Infinity`. El formateador intentaba aplicar `toFixed()` a ese resultado y podia romper la herramienta.

- Se rechazan entradas y resultados no finitos, ademas de las divisiones entre cero.
- El resultado conserva hasta 12 cifras significativas en lugar de redondear siempre a dos decimales.
- Los tres campos tienen etiquetas asociadas; el modo directo o inverso expone su estado y el resultado se anuncia como region viva.
- Las entradas completas pero invalidas muestran una explicacion visible en lugar de fallar silenciosamente.

En produccion, la proporcion directa `4 : 200 = 6 : X` devuelve `300`; el modo inverso devuelve `133,333333333`. Una entrada no finita devuelve `X` y el aviso de rango, sin excepciones. Tras desplegar `759c34c` y `b3bfa24`, canonical, H1, JSON-LD, consola y `lastmod` quedaron verificados. IndexNow recibio la URL con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Fiabilidad del conversor WebP

La revision funcional de `/compresor-webp` detecto que un fallo de lectura o exportacion se presentaba como conversion completada. Ademas, el nombre de descarga usaba el valor actual del control de calidad en lugar del empleado realmente y el contenido prometia ahorros que no se cumplen en todos los archivos.

- Los errores de lectura, Canvas y exportacion muestran ahora una explicacion y nunca un falso estado de exito.
- Se rechazan imagenes superiores a 40 megapixeles antes de crear el lienzo para reducir el riesgo de bloqueo del navegador.
- Cada resultado conserva la calidad utilizada en su nombre de descarga aunque el control cambie despues.
- La interfaz diferencia archivos mas pequenos, mas grandes y sin cambio de tamano; el contenido visible y el JSON-LD ya no garantizan reducciones.
- El control de calidad y las acciones de descarga o eliminacion incorporan nombres accesibles.

ESLint dirigido, TypeScript, `git diff --check` y el build de produccion de 58 rutas finalizaron correctamente. La prueba local y la prueba posterior en produccion confirmaron una salida `red_q80.webp` despues de mover el control a 50, el texto `sin cambio` para `592 B -> 592 B`, el error de una imagen ilegible y el rechazo de un SVG de 42 megapixeles. La consola no mostro errores ni avisos.

Tras desplegar `76b13b2` y `8458647`, la pagina responde `200` y el sitemap publica `2026-08-20T21:07:53.000Z` como `lastmod`. IndexNow recibio solo `/compresor-webp` con `HTTP 200`. El primer reenvio del sitemap a GSC fue rechazado con `HTTP 403` porque `webmasters.readonly` no autoriza escrituras; el alcance `webmasters` correcto recibio `HTTP 204` sin exponer credenciales.

## Fiabilidad del generador de firmas de email

Una nueva consulta de GA4 y GSC mantuvo el diagnostico editorial: cero sesiones organicas en los ultimos siete dias completos y ninguna consulta nueva con demanda suficiente para justificar otro articulo. GSC todavia no reconoce `/generador-firmas-email`; mejorar la utilidad existente tiene menos riesgo que crear otra URL sin senales de busqueda.

La revision funcional detecto tres problemas verificables: todas las firmas incluian `hola@tudominio.com` sin ofrecer un campo para cambiarlo, una web introducida como `https://example.com` generaba `https://https://example.com` y la metadata prometia enlaces sociales inexistentes. Tambien se informaba de compatibilidad universal aunque cada cliente de correo puede modificar estilos o descartar imagenes incrustadas.

- El correo es ahora un campo opcional y solo aparece en la firma cuando el usuario lo introduce.
- Las webs con o sin protocolo producen un enlace HTTPS valido sin duplicarlo.
- La copia usa HTML y texto enriquecido cuando la API moderna esta disponible, conserva un fallback y solo muestra exito cuando la operacion funciona.
- Los campos, el selector de color, la carga de logo y las acciones tienen asociaciones o nombres accesibles.
- El contenido visible, metadata y JSON-LD describen solo las funciones existentes y recomiendan enviar un mensaje de prueba.
- La copia completada se registra en GA4 solo tras consentimiento y sin incluir datos personales de la firma.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. La prueba local y la de produccion confirmaron `mailto:persona@example.com`, `https://example.com` sin protocolo duplicado, el estado `Firma copiada`, ausencia de correo ficticio inicial y ancho de documento de 390 px en un viewport movil de 390 px. Produccion mantiene canonical propio, `WebApplication`, `FAQPage` y consola limpia.

Tras desplegar `1cba026` y `50e8b92`, el sitemap publica `2026-08-20T21:25:29.000Z` como `lastmod`. IndexNow recibio solo `/generador-firmas-email` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`. La busqueda final en el codigo desplegable no encontro Monetag, Moneytag, popunders, OnClickA ni PropellerAds.

## Calculo y tabla completa de prestamos

La inspeccion de GSC mostro `/calculadora-prestamos` como `Descubierta: actualmente sin indexar`. La herramienta ya tenia contenido financiero, referencias del Banco de Espana y una guia editorial especifica, pero un TIN del 0% producia una division `0/0` y anulaba la simulacion. Ademas, la interfaz afirmaba que se podia consultar la tabla completa aunque siempre recortaba el resultado a doce meses.

- El TIN 0% usa ahora una division directa del capital entre las mensualidades.
- Importe, TIN y plazo rechazan valores negativos, no finitos o fuera de limites razonables con mensajes visibles.
- La tabla muestra doce meses inicialmente y permite desplegar todas las mensualidades, hasta el limite de 50 anos.
- La tabla incorpora una descripcion accesible, los campos estan asociados con sus etiquetas y los resultados se anuncian como region viva.
- La guia visible indica correctamente que el plazo se introduce en anos, no en meses.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tanto localmente como en produccion, 10.000 EUR al 0% durante 5 anos produce una cuota de 166,67 EUR, un total de 10.000 EUR y 0 EUR de intereses. La tabla completa contiene 60 filas y termina en el mes 60 con saldo 0,00 EUR. Un importe negativo muestra un error y no genera resultados.

Tras desplegar `6332df5` y `be0dca8`, produccion mantiene canonical propio, ancho de documento de 390 px en viewport movil de 390 px y consola limpia. El sitemap publica `2026-08-20T21:35:17.000Z` como `lastmod`; IndexNow recibio solo `/calculadora-prestamos` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`. El codigo desplegable continua sin Monetag, Moneytag, popunders, OnClickA ni PropellerAds.

## Limites fiables en la calculadora de descuentos

La revision funcional de `/calculadora-descuentos` detecto que aceptaba porcentajes superiores al 100% y precios negativos. Esos valores producian precios finales negativos y la interfaz los describia igualmente como una buena oferta, lo que convertia un error de entrada en una recomendacion comercial enganosa.

- El precio debe ser finito y estar entre 0,01 EUR y 100.000.000 EUR.
- El descuento debe ser finito y estar entre 0% y 100%; un descuento del 100% produce correctamente un precio final de 0 EUR.
- Los valores invalidos muestran un error asociado solo al campo que falla y retiran los resultados anteriores.
- El panel usa una region viva, los botones rapidos exponen su estado y la etiqueta subjetiva `Buena oferta` se sustituyo por `Ahorro calculado`.
- El FAQ visible y `FAQPage` explican de forma coherente que los descuentos sucesivos se aplican uno tras otro; 20% y despues 10% equivalen a 28%, no a 30%.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. Las pruebas locales y de produccion confirmaron 60 EUR al 100% como 0 EUR finales, el bloqueo de 101% y de un precio negativo, ancho de 390 px en viewport movil de 390 px, canonical propio, tres preguntas FAQ sincronizadas y consola limpia. La pagina no carga Monetag, Moneytag, popunders, OnClickA ni PropellerAds.

Tras desplegar `da28d5e` y `dfcf9d7`, el sitemap publica `2026-08-20T21:45:00.000Z` como `lastmod`. IndexNow recibio solo `/calculadora-descuentos` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Cuenta atras fiable en el temporizador

La auditoria de `/temporizador` encontro que los campos aceptaban minutos y segundos negativos, valores vacios podian producir `NaN:NaN` al reiniciar y los segundos no estaban limitados a 59. El conteo restaba uno por cada intervalo ejecutado, por lo que acumulaba retraso cuando el navegador ralentizaba una pestaña en segundo plano.

- La cuenta atras usa una hora objetivo monotona y recalcula el tiempo restante, en lugar de asumir que cada intervalo dura exactamente un segundo.
- Minutos y segundos deben ser enteros validos; la duracion total se limita al intervalo entre 1 segundo y 24 horas.
- Pausa, reanudacion, reinicio, preajustes, silencio y detencion de alarma limpian sus intervalos y conservan estados coherentes.
- El control para detener la alarma permanece estable y no rebota fuera del punto de pulsacion.
- El contador y el final de la alarma se anuncian de forma accesible, mientras que el contenido explica que el sistema operativo puede suspender el sonido en segundo plano.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. Las pruebas locales confirmaron que un contador de 2 segundos llega a `00:00`, activa una alarma que se puede detener y restaura `00:02`; una pausa en `00:05` permanecio inmovil durante dos segundos. En produccion se repitieron el final de 2 segundos y el rechazo de minutos negativos con el campo correcto marcado como invalido. El viewport y el documento miden 390 px, canonical y H1 son unicos, la consola esta limpia y no se cargan scripts de publicidad intrusiva.

Tras desplegar `5946498` y `d95eacb`, el sitemap publica `2026-08-20T21:54:24.000Z` como `lastmod`. IndexNow recibio solo `/temporizador` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Vueltas parciales fiables en el cronometro

La revision de `/cronometro` detecto que cada vuelta guardaba solo el tiempo total acumulado, por lo que no permitia comparar la duracion de cada intervalo. El cronometro tambien dependia de `Date.now()`, actualizaba el estado cada 10 ms y prometia precision absoluta, alarma y pantalla completa pese a no ofrecer esas funciones.

- El tiempo transcurrido se calcula con el reloj monotono del navegador y la pantalla se actualiza mediante `requestAnimationFrame`.
- Pausar y reanudar conserva el tiempo acumulado sin reiniciar ni sumar el periodo detenido.
- Cada vuelta muestra por separado su intervalo y su tiempo total; el registro se limita a 500 vueltas para acotar memoria.
- Las acciones tienen nombres accesibles, el contador usa semantica de temporizador y la tabla de vueltas expone columnas claras.
- Metadata, Open Graph, Twitter, JSON-LD y contenido visible describen solo pausa, reinicio, centesimas y vueltas reales; se retiraron alarma, pantalla completa y precision absoluta.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. En produccion se registraron dos vueltas con intervalos `00:00.80` y `00:00.75` y totales `00:00.80` y `00:01.55`; tras pausar, `00:01.57` permanecio inmovil durante otro segundo. La pagina conserva canonical propio, ancho de 390 px en viewport movil de 390 px, consola limpia y ningun script de publicidad intrusiva.

Tras desplegar `5e35831` y `4007ff8`, el sitemap publica `2026-08-20T22:03:17.000Z` como `lastmod`. IndexNow recibio solo `/cronometro` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Conversion Unicode de mayusculas y minusculas

La auditoria de `/mayusculas-minusculas` detecto que la capitalizacion dependia de `\b` y `\w`, clases ASCII que no reconocen correctamente letras espanolas. Palabras como `arbol` con tilde inicial podian capitalizar una letra interior y las frases iniciadas por signos invertidos no se transformaban como se esperaba. La herramienta tampoco ofrecía una accion directa para copiar el resultado.

- Mayusculas y minusculas usan conversion localizada en espanol.
- La capitalizacion de palabras reconoce letras Unicode, tildes, dieresis y la ene con virgulilla.
- El formato oracion reconoce inicios de texto, cierres de frase, signos invertidos y saltos de linea.
- Se incorporaron copia con fallback, limpieza, contador de caracteres y estados accesibles; las acciones vacias quedan deshabilitadas.
- El contenido explica los limites de la capitalizacion automatica y acota correctamente la afirmacion de privacidad al servidor de CajaUtil.
- La metadata incorpora Twitter Card y JSON-LD declara un sistema operativo compatible veraz.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. Localmente y en produccion, `árbol ÑANDÚ pingüino / ¿qué tal? ¡bien! "última oración"` produjo `Árbol ñandú pingüino / ¿Qué tal? ¡Bien! "Última oración"`, conservando todos los diacriticos. La copia mostro exito solo tras resolverse la escritura. La pagina mantiene 390 px de ancho en un viewport de 390 px, canonical propio, consola limpia y ningun script de publicidad intrusiva.

Tras desplegar `896f132` y `b534a17`, el sitemap publica `2026-08-20T22:10:06.000Z` como `lastmod`. IndexNow recibio solo `/mayusculas-minusculas` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Operaciones completas en la calculadora de porcentajes

La auditoria de `/calculadora-porcentajes` encontro una discrepancia entre el contenido editorial y la herramienta: la pagina prometia calcular proporciones y variaciones, pero la interfaz solo obtenia un porcentaje de una cantidad. El resultado tambien se limitaba a dos decimales, por lo que valores pequenos como el 0,001% de 1 se mostraban como 0.

- La herramienta permite calcular un porcentaje de una cantidad, la proporcion entre una parte y un total y la variacion entre un valor inicial y otro final.
- Los divisores iguales a cero, los valores iniciales no positivos y los numeros no finitos muestran errores asociados al campo correspondiente.
- Los resultados conservan hasta doce cifras significativas para evitar redondeos que cambien el significado del calculo.
- Los modos exponen su estado, los errores y resultados usan regiones accesibles y cada operacion muestra su formula.
- El contenido visible, HowTo y `WebApplication` describen solo las operaciones disponibles.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. En local se verificaron `21% de 1500 = 315`, `25 de 200 = 12,5%`, `100 a 120 = +20%`, `120 a 100 = -16,6666666667%` y `0,001% de 1 = 0,00001`; un total cero y un valor inicial cero o negativo muestran errores. La vista movil de 390 px no presenta desbordamiento. Produccion repitio el calculo `315`, mantiene canonical propio, `index, follow`, un H1, JSON-LD valido y consola limpia.

Tras desplegar `a95a76a` y `566d21d`, el sitemap publica `2026-08-20T22:23:43.000Z` como `lastmod`. IndexNow recibio solo `/calculadora-porcentajes` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Generacion QR segura y flujo correcto desde WhatsApp

La auditoria del generador QR reprodujo una caida completa de la herramienta: `qrcode.react` lanza `RangeError: Data too long` al intentar renderizar 1.300 caracteres ASCII con correccion de errores H. El enlace `Sacar QR Code` de la herramienta de WhatsApp tampoco transferia el contenido y el QR resultante siempre apuntaba a la portada de CajaUtil.

- El generador mide el contenido en UTF-8 y admite un maximo conservador de 1.200 bytes, verificado con texto ASCII y caracteres multibyte.
- Los contenidos vacios o demasiado largos muestran un error asociado al campo, retiran la vista previa y desactivan la descarga sin provocar una excepcion.
- La conversion a PNG comprueba la disponibilidad del lienzo y comunica los fallos de carga o exportacion.
- El parametro `text` rellena ahora el generador con el enlace compartido desde WhatsApp.
- WhatsApp genera una URL `wa.me` real, elimina la opcion internacional que anadia un prefijo `00` invalido y asocia etiquetas accesibles con telefono y mensaje.
- La metadata QR incorpora Twitter Card y ambos `WebApplication` describen funciones y categorias reales.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. En local y produccion, 1.300 bytes muestran el limite de 1.200 sin renderizar el SVG ni permitir la descarga. El flujo con `+34`, `600 123 456` y `Hola CajaUtil` genera `https://wa.me/34600123456?text=Hola%20CajaUtil`; `BarcodeDetector` recupero exactamente ese valor desde el QR. Produccion mantiene 390 px de documento en un viewport de 390 px, canonical propio, `index, follow`, un H1, JSON-LD valido y consola limpia.

Tras desplegar `8969ef7` y `a4120ba`, el sitemap publica `2026-08-20T22:38:27.000Z` para `/generador-qr` y `/generador-enlace-whatsapp`. IndexNow recibio las dos URL con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`. La auditoria transversal no encontro Monetag, Moneytag, popunders, OnClickA, PropellerAds ni otros scripts publicitarios intrusivos en el codigo desplegable.

## Senales coherentes en paginas no encontradas

Una URL inexistente devolvia correctamente una respuesta 404 y `noindex, nofollow`, pero heredaba de la raiz el titulo, canonical, Open Graph y Twitter de la portada. Esa combinacion describia un error como si fuera una copia de `https://cajautil.com/` y enviaba una senal canonical contradictoria.

El catch-all y la pagina 404 anulan ahora `alternates`, Open Graph y Twitter, y declaran un titulo y una descripcion especificos. ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `5c741db`, una URL aleatoria en produccion conserva `noindex, nofollow, nocache`, muestra `Pagina no encontrada` y no contiene canonical ni tarjetas sociales heredadas.

## Edad calendaria sin componentes negativos

La calculadora de edad restaba por separado anos, meses y dias. Cuando el dia de nacimiento no existia en el mes anterior, una sola operacion de prestamo no normalizaba el resultado: `31/01/2000` frente a `01/03/2026` producia `26 anos, 1 mes y -2 dias`. Un cumpleaños en el dia actual tambien apuntaba a la medianoche ya pasada y mostraba `-1 dias restantes`.

- La edad se calcula ahora desde el ultimo aniversario real y avanza por meses con el dia limitado al ultimo dia valido de cada mes.
- El 29 de febrero mantiene la politica visible de usar el ultimo dia de febrero en anos no bisiestos.
- Un cumpleaños actual muestra `¡Es hoy!` y una cuenta a cero en lugar de un intervalo negativo.
- Las fechas futuras e invalidas retiran resultados y muestran un error asociado al campo.
- El selector de fecha tiene etiqueta y descripcion accesibles, y la metadata incorpora Twitter Card propia.

Las pruebas reproducibles confirmaron `31/01/2000 → 01/03/2026 = 26 anos, 1 mes, 1 dia`, `21/08/1990 → 21/08/2026 = 36 anos` y `29/02/2000 → 28/02/2026 = 26 anos`. ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. Produccion repitio el cumpleaños actual como `36/0/0` y `¡Es hoy!`, con canonical propio, `index, follow`, un H1, 390 px de documento en viewport de 390 px y consola limpia.

Tras desplegar `89b4859` y `50e8302`, el sitemap publica `2026-08-20T22:50:49.000Z` para `/calculadora-edad`. IndexNow recibio solo esa URL con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Simulacion acotada del interes compuesto

La auditoria de `/calculadora-interes-compuesto` detecto que los campos solo rechazaban `NaN`. Un plazo arbitrariamente grande ejecutaba doce iteraciones por ano en el hilo principal y podia bloquear el navegador; tambien se aceptaban importes negativos y resultados no finitos. Una tasa negativa se mostraba con el prefijo incorrecto `+-` y el rendimiento conservaba el color de una ganancia.

- Capital inicial y aportacion mensual deben ser finitos y no negativos, con maximos de 100.000.000 EUR y 1.000.000 EUR respectivamente.
- La tasa anual admite escenarios entre -100% y 100%; el plazo debe ser un entero entre 1 y 50 anos, por lo que el calculo queda acotado a 600 periodos mensuales.
- Se rechaza una simulacion con capital y aportacion iguales a cero, y se comprueban resultados no finitos antes de representarlos.
- Los errores quedan asociados al campo correspondiente y el rendimiento negativo usa signo y color de perdida.
- La descripcion y Twitter Card presentan la herramienta como una simulacion orientativa, no como una promesa de ganancias.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. Localmente y en produccion, 5.000 EUR iniciales, 200 EUR mensuales y 7% durante 10 anos producen 44.665,27 EUR finales, 29.000 EUR aportados y +15.665,27 EUR de rendimiento. Con -10% se muestran 17.039,52 EUR finales y -11.960,48 EUR de rendimiento; un plazo de 1.000.000.000 de anos se rechaza inmediatamente y ambos aportes a cero muestran un error claro.

Tras desplegar `f4ee641` y `12bf6b8`, produccion responde `HTTP 200`, conserva canonical propio, `index, follow`, un H1, JSON-LD valido, ancho de 390 px en un viewport de 390 px y consola limpia. El sitemap publica `2026-08-20T23:00:00.000Z` como `lastmod`. IndexNow recibio solo `/calculadora-interes-compuesto` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`. La busqueda final no encontro Monetag, Moneytag, popunders, OnClickA ni PropellerAds en el codigo desplegable.

## Lectura QR eficiente y segura desde fotos grandes

La consulta actualizada de GSC para los ultimos 28 dias muestra 190 impresiones para `/calculadora-sueldo-neto` y 11 para `/lector-qr`; las consultas del lector se concentran en leer o escanear codigos desde fotos e imagenes. La inspeccion individual confirma que `/lector-qr` esta enviada e indexada, permite indexacion y usa la misma canonical declarada y elegida por Google. GA4 registra cero sesiones organicas en los ultimos siete dias completos y todavia no recibe eventos de herramientas. No aparece una intencion diferenciada que justifique publicar otro articulo sin riesgo de contenido debil o canibalizacion.

La revision funcional del lector detecto que una imagen admitida podia convertirse en un lienzo de hasta 20 megapixeles y analizarse completa con `jsQR` en el hilo principal. Las fotos modernas de mas de 20 MP se rechazaban, mientras que las cercanas al limite podian consumir mucha memoria. La copia tampoco esperaba el resultado del portapapeles y registraba una finalizacion aunque la escritura fallase.

- Las fotos fuente de hasta 32 MP se cargan mediante una URL de objeto y se reducen a un maximo de 4 MP antes del analisis.
- Las URL de objeto se liberan y cada lectura tiene un identificador para impedir que una carga antigua sustituya un resultado nuevo.
- El estado de analisis se comunica de forma visible y accesible; los archivos invalidos, imagenes sin QR y resoluciones excesivas conservan errores especificos.
- Cualquier resultado se puede copiar con fallback y solo se registra el evento cuando la copia termina correctamente.
- Las URL HTTP o HTTPS muestran el dominio y una advertencia contra enlaces enganosos antes de ofrecer su apertura.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. Localmente y en produccion, una foto sintetica de `6000 x 4000` px con un QR se redujo a `2449 x 1633` px y devolvio exactamente `https://example.com/ruta?utm_source=qr-test`; la interfaz mostro `example.com`, permitio copiar y mantuvo 390 px de documento en un viewport de 390 px. Una imagen sin QR, un archivo JSON y una imagen de 36 MP produjeron sus errores correspondientes sin excepciones.

Tras desplegar `b533c36` y `f4600c6`, produccion responde `HTTP 200`, mantiene canonical propio, `index, follow`, un H1, JSON-LD valido y consola limpia. El sitemap publica `2026-08-20T23:13:25.000Z` como `lastmod`. IndexNow recibio solo `/lector-qr` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`. No se encontro codigo de Monetag, Moneytag, popunders, OnClickA ni PropellerAds.

## Conversion salarial bidireccional y pagas bien diferenciadas

La auditoria de `/calculadora-sueldo-neto`, priorizada por sus 190 impresiones en GSC, detecto una ambiguedad funcional importante: al seleccionar 14 pagas, la interfaz llamaba "neto mensual" al resultado de dividir el neto anual entre 14. Esa cifra representa el importe por paga, no el promedio mensual. Las consultas observadas tambien incluian salario bruto mensual, 12 o 14 pagas y conversion de neto a bruto, pero la herramienta solo admitia bruto anual a neto.

- La calculadora convierte ahora de bruto a neto y de neto a bruto con los porcentajes editables de IRPF y cotizacion.
- El usuario puede introducir un importe anual o mensual/por paga y elegir 12 o 14 pagas.
- El desglose diferencia bruto anual, neto anual, bruto por paga, neto por paga y promedio neto mensual calculado sobre doce meses.
- Los importes vacios, no positivos, no finitos o superiores a 10.000.000 EUR anuales no producen resultados engañosos y muestran errores asociados al campo.
- Metadata, HowTo, FAQ visible y FAQ estructurada explican la conversion inversa sin crear otra URL que pueda canibalizar la intencion principal.

Los casos reproducibles confirmaron que 30.000 EUR brutos con 15% de IRPF y 6,5% de cotizacion producen 23.550 EUR netos anuales: 1.962,50 EUR por paga en 12 pagas, 1.682,14 EUR por paga en 14 y 1.962,50 EUR de promedio mensual en ambos repartos. La conversion inversa de 1.500 EUR netos por paga en 14 pagas devuelve 26.751,59 EUR brutos anuales. Un importe anual de 10.000.001 EUR se rechaza con un error accesible.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `2632ce0` y `dd8a92d`, produccion mantiene canonical propio, `index, follow`, un H1, seis bloques JSON-LD validos, 390 px de documento en un viewport de 390 px y consola limpia. El sitemap publica `2026-08-20T23:27:44.000Z` como `lastmod`. IndexNow recibio solo `/calculadora-sueldo-neto` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Enlazado contextual rastreable entre herramientas y guias

La inspeccion individual de todas las URLs del sitemap en GSC encontro 5 URLs enviadas e indexadas, 16 descubiertas sin indexar, 19 que Google todavia no reconoce y 2 rastreadas sin indexar. El rastreo interno posterior detecto que las guias definidas en `toolEditorialContent` no aparecian en las herramientas que usaban la seccion editorial compacta: el modo `compact` ocultaba deliberadamente todo el bloque de enlaces relacionados.

Los datos de busqueda no justifican publicar otro articulo. Entre el 24 de julio y el 20 de agosto, `/calculadora-sueldo-neto` concentra 190 impresiones, `/lector-qr` 11 y el resto de URLs solo 10 en conjunto. La guia de interes compuesto suma 2 impresiones en posicion media 8, pero GSC la rastreo el 20 de agosto a las 01:26, antes de su ultima revision editorial; se prioriza facilitar el nuevo rastreo en lugar de volver a editarla sin una consulta visible que lo justifique.

- Las 12 herramientas con una o mas guias relacionadas muestran ahora un bloque compacto y visible con enlaces HTML directos.
- Se refuerzan los grupos de sueldo neto, IVA, prestamos, hipotecas, IBAN, IMC/calorias, interes compuesto, WebP, codigos QR y contrasenas.
- Cada destino sigue siendo una guia estatica, canonica e indexable ya presente en el sitemap; no se crean variantes ni paginas puerta.
- Los 12 `lastmod` de las herramientas afectadas usan la fecha UTC real del commit funcional: `2026-08-20T23:40:10.000Z`.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. En produccion, las 12 herramientas contienen sus enlaces esperados en el HTML inicial. `/calculadora-imc` conserva canonical propio, `index, follow`, un H1, 390 px de documento en un viewport de 390 px y consola limpia. IndexNow recibio las 12 URLs con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`. La busqueda del codigo desplegable no encontro Monetag, Moneytag, popunders, OnClickA ni PropellerAds.

## Cuotas de amortizacion hipotecaria y validacion numerica

La revision de `/calculadora-hipotecas` encontro que los campos convertian inmediatamente cada entrada a numero. Eso impedia conservar campos vacios y el valor `0%`, y permitia plazos decimales o resultados no finitos sin una explicacion accesible. La herramienta tampoco mostraba como se repartia cada pago entre capital e intereses.

- Importe, plazo y TIN conservan el texto introducido y muestran errores asociados al campo.
- El importe queda acotado entre 0,01 EUR y 10.000.000 EUR, el plazo a enteros de 1 a 50 anos y el TIN entre 0% y 100%.
- Los resultados no finitos se descartan y un TIN del 0% usa la division lineal correcta.
- Se muestran las primeras 12 cuotas con pago, capital, intereses y saldo pendiente.
- La metadata describe la cuota y la amortizacion, y el contenido evita presentar porcentajes de financiacion o gastos como reglas universales.
- El contenedor principal puede reducirse por debajo de su ancho de contenido; la tabla conserva scroll horizontal propio sin ampliar el documento movil.

Las pruebas reproducibles confirmaron que 150.000 EUR a 25 anos y 3,5% producen una cuota de 750,94 EUR, 75.280,61 EUR de intereses y un coste total de 225.280,61 EUR. Con TIN 0%, la cuota es 500 EUR, los intereses son cero y cada una de las primeras cuotas amortiza 500 EUR. Un plazo de 1,5 anos y un importe de cero muestran errores accesibles y retiran los resultados.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `1ff6233` y `de19090`, produccion mantiene canonical propio, `index, follow`, un H1, JSON-LD valido, 12 filas de amortizacion, 390 px de documento en un viewport de 390 px y consola limpia. El sitemap publica `2026-08-20T23:53:43.000Z` como `lastmod`. IndexNow recibio solo `/calculadora-hipotecas` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Conversiones precisas y limites fisicos validos

La auditoria de `/conversor-unidades` reprodujo tres resultados incorrectos: `1 mg` a toneladas se redondeaba a cero, las temperaturas inferiores al cero absoluto producian valores numericos y una conversion extrema podia mostrar `Infinity`. Las equivalencias anglosajonas tambien usaban factores truncados y la interfaz no comunicaba los errores de forma accesible.

- Los resultados conservan hasta 12 cifras significativas y usan notacion cientifica para magnitudes extremas legibles.
- La conversion rechaza entradas y resultados no finitos antes de representarlos.
- Las temperaturas inferiores a `-273,15 °C` se rechazan independientemente de la escala de entrada; el cero absoluto exacto sigue siendo valido.
- Los factores de libras, onzas, galones US, tazas US y millas por hora usan equivalencias mas precisas.
- Inputs, selectores, categorias, intercambio, errores y resultado tienen nombres y estados accesibles.
- El contenido explica precision y redondeo sin prometer exactitud universal, y enlaza las referencias oficiales del BIPM y NIST.

Las pruebas reproducibles confirmaron `1 mg = 1E-9 toneladas`, `-459,67 °F = 0 K` y el rechazo accesible de `-500 °F` y de resultados fuera del rango numerico. ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente.

Tras desplegar `ab50e8b` y `65b1f4f`, produccion mantiene canonical propio, `index, follow`, un H1, cinco bloques JSON-LD validos, Twitter Card propia, 390 px de documento en un viewport de 390 px y consola limpia. El sitemap publica `2026-08-21T00:11:03.000Z` como `lastmod`. IndexNow recibio solo `/conversor-unidades` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Recuento linguistico de palabras y tiempo de lectura coherente

La auditoria de `/contador-de-palabras`, unica herramienta indexable sin revision individual ni `lastmod`, encontro que el calculo separaba el texto solo por espacios. Por ello, `...` contaba como una palabra, `— 😊` como dos y un texto vacio mostraba menos de un minuto de lectura. La metadata tambien prometia funcionamiento offline sin existir un service worker que garantizara la recarga sin conexion.

- El recuento usa segmentacion Unicode para el idioma espanol y solo suma segmentos identificados como palabras o numeros.
- Signos de puntuacion y emojis aislados no aumentan el numero de palabras.
- Tildes, apostrofos, numeros y saltos de linea conservan un recuento linguistico coherente.
- El texto vacio muestra cero minutos; de 1 a 199 palabras muestra menos de un minuto y 200 palabras muestran un minuto.
- `useDeferredValue` mantiene la escritura prioritaria mientras se analizan textos grandes, y el estado accesible comunica cuando las estadisticas se estan actualizando.
- La descripcion elimina la promesa offline y explica que el texto no se envia; la pagina incorpora Twitter Card y una definicion visible del criterio de recuento.

Las pruebas locales y de produccion confirmaron que `... — 😊` cuenta cero palabras y que `Hola, pingüino. L'été 2026` seguido de `CajaUtil` en otra linea cuenta cinco. Un texto de 200 palabras devuelve 1 minuto. ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente.

Tras desplegar `6f01e81` y `c777d43`, produccion mantiene canonical propio, `index, follow`, un H1, cuatro bloques JSON-LD validos, Twitter Card propia, 390 px de documento en un viewport de 390 px y consola limpia. El sitemap publica `2026-08-21T00:25:43.000Z` como `lastmod`. IndexNow recibio solo `/contador-de-palabras` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Calculo de IVA acotado y sin resultados infinitos

La auditoria funcional de `/calculadora-iva` encontro que los limites HTML no se comprobaban en el calculo. Un tipo negativo generaba cuotas negativas y, al quitar un IVA de `-100%`, la division entre cero mostraba valores infinitos. Los errores reiniciaban el desglose a cero sin explicar el motivo y las etiquetas no estaban asociadas a sus campos.

- Importe y porcentaje conservan el texto introducido, incluidos los campos vacios y el valor `0%`.
- El importe debe ser finito, superior a cero y no mayor de 1.000.000.000.000 EUR; el porcentaje debe estar entre 0% y 100%.
- El calculo comprueba que base, cuota y total sean finitos antes de representarlos.
- Los resultados usan siempre dos decimales y se apilan en movil para admitir importes grandes sin ampliar el documento.
- Modos y tipos habituales exponen su estado seleccionado; inputs, errores y resultados tienen nombres, asociaciones y regiones vivas accesibles.
- El contenido aclara que un porcentaje personalizado es una simulacion aritmetica y no determina el tipo legal aplicable, que debe contrastarse con las referencias de la AEAT ya publicadas.

Las pruebas locales y de produccion confirmaron `100 EUR + 21% = 121 EUR` y que quitar 21% de 100 EUR devuelve 82,64 EUR de base y 17,36 EUR de cuota. Un tipo de `-100%` y un importe superior al maximo retiran el resultado y muestran errores asociados; `0%` produce base y total de 100 EUR con cuota cero. El maximo permitido con 100% muestra un total de 2.000.000.000.000 EUR sin desbordamiento horizontal.

ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `a8f6633` y `2f6ab9c`, produccion mantiene canonical propio, `index, follow`, un H1, cuatro bloques JSON-LD validos, Twitter Card propia, 390 px de documento en un viewport de 390 px y consola limpia. El sitemap publica `2026-08-21T00:36:37.000Z` como `lastmod`. IndexNow recibio solo `/calculadora-iva` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Estimacion calorica reactiva y correctamente descrita

La auditoria funcional de `/calculadora-calorias` encontro que el resultado se guardaba separado de los datos del formulario. Cambiar el sexo o el nivel de actividad despues de calcular dejaba visible una cifra obsoleta. Ademas, `parseInt` aceptaba edades decimales truncandolas y la pagina atribuia el TDEE completo a Mifflin-St Jeor, aunque esa ecuacion estima el metabolismo basal y el gasto diario se obtiene despues mediante un factor de actividad.

- Tras la primera estimacion, BMR y TDEE se recalculan al cambiar sexo, edad, peso, altura o actividad.
- La edad debe ser un entero entre 18 y 100 anos; peso y altura deben ser numeros finitos dentro de 30-300 kg y 120-230 cm.
- Una entrada invalida retira inmediatamente el resultado anterior y muestra errores asociados a cada campo.
- Las selecciones exponen `aria-pressed` y los resultados se comunican mediante una region viva.
- La interfaz, metadata, Twitter Card y JSON-LD distinguen la ecuacion de BMR del factor orientativo usado para estimar TDEE.

La prueba movil con 30 anos, 70 kg y 175 cm devolvio para hombre con actividad moderada `1649/2556 kcal`; al cambiar a mujer se actualizo sin otro clic a `1483/2298 kcal`, y al seleccionar actividad sedentaria paso a `1483/1779 kcal`. Introducir `30,5` anos retiro el resultado y comunico que la edad debe ser entera.

ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `f0375be` y `1dd891f`, produccion conserva canonical propio, metadata social completa, 390 px de viewport y consola limpia. El sitemap publica `2026-08-21T00:48:11.000Z` como `lastmod`. IndexNow recibio solo `/calculadora-calorias` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Validacion completa de los controles del IBAN espanol

La auditoria de `/validador-iban` encontro que la herramienta solo aplicaba MOD-97 y eliminaba cualquier simbolo antes del calculo. Por ello, un IBAN recalculado para superar MOD-97 pero con los controles internos del CCC alterados se marcaba como correcto, y una entrada con caracteres no admitidos podia validarse tras ocultar silenciosamente esos caracteres.

- La validacion conserva MOD-97 y comprueba tambien los dos digitos de control internos del Codigo Cuenta Cliente espanol.
- Solo se toleran letras, numeros, espacios y guiones; el resto de caracteres produce un error visible.
- Un resultado correcto identifica los codigos declarados de entidad y oficina sin afirmar que la cuenta exista o pertenezca al destinatario.
- El campo expone su estado invalido y asocia los mensajes; resultados y avisos usan regiones accesibles.
- Interfaz, contenido editorial, metadata, Twitter Card y JSON-LD describen exactamente las comprobaciones realizadas.

El IBAN publico de ejemplo `ES91 2100 0418 4502 0005 1332` supera MOD-97 y CCC. La variante sintetica `ES56 2100 0418 4402 0005 1332`, recalculada para superar MOD-97 pero con un control nacional incorrecto, ahora se rechaza; la misma entrada valida terminada en `!` tambien se rechaza en lugar de ignorar el simbolo.

ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `f0ffb96` y `f074526`, produccion mantiene canonical propio, `index, follow`, Twitter Card propia, JSON-LD valido, 390 px de documento en un viewport de 390 px y consola limpia. El sitemap publica `2026-08-21T01:00:00.000Z` como `lastmod`. IndexNow recibio solo `/validador-iban` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

La consulta de crecimiento no justifica un articulo adicional: GSC sigue concentrando la demanda de 90 dias en sueldo neto (665 impresiones) y lector QR (36), mientras GA4 registra cero sesiones organicas en los ultimos siete dias completos. Se prioriza mejorar herramientas existentes frente a crear contenido sin intencion diferenciada. La busqueda en `src/` no encontro Moneytag, Monetag, popunders, OnClickA, PropellerAds ni Adsterra.

## IMC decimal, validado y sin diagnosticos implicitos

La inspeccion de GSC previa a la mejora mostraba `/calculadora-imc` como `Google no reconoce esta URL`, sin rastreo, canonical elegida, URL referente ni sitemap asociado todavia. La auditoria funcional encontro ademas que la herramienta solo admitia valores enteros mediante sliders y describia el rango central como `Peso Normal` y el resultado como saludable, una conclusion que el IMC no permite establecer por si solo.

- Peso y altura admiten decimales y conservan campos editables en lugar de forzar valores enteros.
- Se validan finitud y rangos de 30-300 kg y 100-250 cm; una entrada invalida retira el resultado anterior.
- Las categorias usan los limites de referencia para adultos y evitan presentar el IMC como diagnostico individual.
- Para cada altura se muestra la traduccion matematica del intervalo 18,5-24,9 a peso, aclarando que no es un objetivo personal ni una recomendacion.
- Inputs, errores y resultado tienen asociaciones y regiones vivas accesibles; las clases visuales son estaticas y predecibles.
- Open Graph y Twitter Card describen la estimacion de forma prudente.

La prueba local y de produccion con `72,5 kg` y `174,3 cm` devolvio IMC `23,86` y un intervalo matematico aproximado de `56,2-75,6 kg`. Una entrada inferior al minimo retiro el resultado y mostro el error asociado.

ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `bde18aa` y `2a1b734`, produccion mantiene canonical propio, `index, follow`, Twitter Card propia, JSON-LD valido, 390 px de documento en un viewport de 390 px y consola limpia. El sitemap publica `2026-08-21T01:11:34.000Z` como `lastmod`. IndexNow recibio solo `/calculadora-imc` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Generador de contrasenas: promesas verificables y copia fiable

La inspeccion de GSC previa al cambio mostraba `/generador-contrasenas` como `Google no reconoce esta URL`, sin rastreo, canonical elegida, URL referente ni sitemap asociado. La herramienta generaba contrasenas localmente mediante `crypto.getRandomValues`, pero la metadata prometia funcionamiento `offline` sin existir un service worker que garantizara la carga sin conexion. El boton de copia tambien mostraba exito antes de que `Clipboard API` confirmara la escritura.

- El titulo y Open Graph ya describen generacion online en el navegador sin prometer carga offline.
- Se incorporo una Twitter Card propia y coherente con la funcionalidad visible.
- El estado `Copiado` solo aparece tras completarse `navigator.clipboard.writeText`.
- Si el navegador o sus permisos rechazan la copia, se muestra y anuncia una instruccion para seleccionar y copiar manualmente.
- Se conserva la seleccion aleatoria sin sesgo por rechazo y la garantia de incluir al menos un caracter de cada grupo elegido.

La prueba local confirmo tanto la copia correcta como el manejo de un `NotAllowedError` simulado. ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `107e5d3` y `3617cdd`, produccion mantiene canonical propio, `index, follow`, Twitter Card, JSON-LD valido, 390 px de documento en un viewport de 390 px y consola limpia. La traza de red no contiene Moneytag, popunders ni redes publicitarias, y la generacion/copia no envia la contrasena. El sitemap publica `2026-08-21T08:15:31.000Z` como `lastmod`. IndexNow recibio solo `/generador-contrasenas` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Descuentos sucesivos y limites monetarios coherentes

GSC mostraba `/calculadora-descuentos` como `Descubierta: actualmente sin indexar`, ya asociada al sitemap y con `/calculadora-regla-de-tres` como URL referente. La auditoria funcional encontro que la interfaz afirmaba admitir precios desde `0,01 EUR`, pero aceptaba `0,001 EUR` y lo redondeaba a `0,00 EUR`. La FAQ tambien explicaba dos descuentos sucesivos, aunque la herramienta obligaba a repetir manualmente el calculo.

- El precio se valida ahora realmente entre `0,01` y `100.000.000 EUR`.
- Un segundo descuento opcional se aplica sobre el precio ya rebajado, no se suma al primer porcentaje.
- El resultado muestra precio final, ahorro y porcentaje acumulado cuando existen dos rebajas.
- Se validan ambos porcentajes entre 0% y 100% y se retiran resultados si cualquier entrada es invalida.
- Metadata, Open Graph, Twitter, WebApplication y FAQPage describen el comportamiento visible.
- `Resumen del ahorro` pasa de H3 a H2 para mantener una jerarquia coherente tras el H1.

Las pruebas local y de produccion con `100 EUR`, `20%` y `10%` devolvieron `72,00 EUR`, `28,00 EUR` de ahorro y `28%` acumulado. `0,001 EUR` y un segundo descuento del `101%` se rechazaron sin conservar resultados. ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `21fb583` y `52566fb`, produccion mantiene canonical propio, `index, follow`, Twitter Card, JSON-LD valido, 390 px de documento en un viewport de 390 px y consola limpia. La traza no contiene Moneytag, popunders ni redes publicitarias. El sitemap publica `2026-08-21T08:22:34.000Z` como `lastmod`. IndexNow recibio solo `/calculadora-descuentos` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Porcentajes: hallar el total sin crear otra URL

GSC mostraba `/calculadora-porcentajes` como `Descubierta: actualmente sin indexar`, asociada al sitemap y referenciada desde `https://www.cajautil.com/sitemap.xml`. Las tres operaciones existentes eran correctas, pero faltaba una intencion distinta y frecuente: hallar la cantidad total cuando se conoce una parte y el porcentaje que representa. Se incorpora dentro de la misma canonical para ampliar la respuesta sin crear una pagina canibal.

- El cuarto modo calcula `cantidad parcial / porcentaje x 100`.
- Un porcentaje igual a cero muestra un error asociado al campo y retira el resultado.
- El selector de operaciones mantiene cuatro opciones accesibles mediante `aria-pressed` y una distribucion movil sin desbordamiento.
- Metadata, Open Graph, Twitter, WebApplication, HowTo, texto visible, ejemplo y FAQ describen las cuatro operaciones.
- Se conservan sin cambios las formulas verificadas de porcentaje de una cantidad, proporcion y variacion porcentual.

Las pruebas local y de produccion confirmaron que si `30` representa el `20%`, la cantidad total es `150`; con `0%` no se conserva resultado. ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `8c9c053` y `e9fb8f4`, produccion mantiene canonical propio, `index, follow`, Twitter Card, JSON-LD valido, 390 px de documento en un viewport de 390 px y consola limpia. La traza no contiene Moneytag, popunders ni redes publicitarias. El sitemap publica `2026-08-21T08:29:10.000Z` como `lastmod`. IndexNow recibio solo `/calculadora-porcentajes` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Edad: fecha de proximo cumpleanos y precision honesta

GSC mostraba `/calculadora-edad` como `Google no reconoce esta URL`, sin rastreo, canonical elegida, referentes ni sitemap asociado. El calculo por calendario y el tratamiento documentado del 29 de febrero eran correctos, pero la interfaz presentaba horas, minutos y segundos como totales exactos pese a no solicitar hora de nacimiento. El contenido editorial tambien mencionaba semanas y fechas objetivo que la herramienta no calcula.

- Se muestra la fecha completa del proximo cumpleanos junto a la cuenta atras.
- Dias, horas, minutos y segundos transcurridos se identifican como aproximaciones.
- El contenido ya describe solo la comparacion con la fecha actual y las unidades realmente visibles.
- Se elimina la afirmacion de horas trabajadas, que no podia derivarse de una fecha de nacimiento.
- Los subtitulos internos pasan de H4 a H3 para mantener la jerarquia despues del H2.
- Se conserva el ajuste explicito del 29 de febrero al ultimo dia de febrero en anos no bisiestos.

Las pruebas de funciones confirmaron que una persona nacida el `22/08/2000` tiene `25 anos, 11 meses y 30 dias` el `21/08/2026`; la interfaz local y de produccion mostro `Sabado, 22 de agosto de 2026` como siguiente cumpleanos. ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `e2c9f3c` y `119dca7`, produccion mantiene canonical propio, `index, follow`, Twitter Card, JSON-LD valido, 390 px de documento en un viewport de 390 px y consola limpia. La traza no contiene Moneytag, popunders ni redes publicitarias. El sitemap publica `2026-08-21T08:37:59.000Z` como `lastmod`. IndexNow recibio solo `/calculadora-edad` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## WhatsApp: confirmacion real al copiar el enlace

GSC muestra `/generador-enlace-whatsapp` como `Descubierta: actualmente sin indexar`, asociada al sitemap y con `/calculadora-imc` como URL referente. La revision funcional reprodujo un falso exito en la accion principal: el boton cambiaba a `Copiado` sin esperar la promesa de Clipboard API. Cuando el navegador rechazaba `writeText` con `NotAllowedError`, el portapapeles conservaba el valor anterior, la interfaz afirmaba que la copia habia funcionado y la consola registraba una promesa rechazada sin controlar.

- La confirmacion aparece solo despues de que `navigator.clipboard.writeText` se resuelva.
- Si la API no esta disponible o rechaza el permiso, se muestra una alerta que indica seleccionar y copiar el enlace manualmente.
- El error desaparece al completar un nuevo intento correctamente.
- El boton tiene un nombre accesible tambien en movil, donde el texto visual permanece oculto.

Las pruebas local y de produccion confirmaron ambos caminos con `https://wa.me/34600123456?text=Hola`: el rechazo no activa el estado de copia y muestra la alternativa manual; el exito escribe exactamente esa URL y anuncia `Enlace copiado`. ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `dafccd1` y `0402142`, produccion mantiene canonical propio, `index, follow`, JSON-LD valido, 390 px de documento en un viewport de 390 px y consola limpia. La traza contiene solo recursos propios, sin Moneytag, popunders ni redes publicitarias. El sitemap publica `2026-08-21T08:49:59.000Z` como `lastmod`. IndexNow recibio solo `/generador-enlace-whatsapp` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## QR: densidad segura en todas las descargas PNG

GSC confirma `/generador-qr` como `Enviada e indexada`, con rastreo correcto y coincidencia entre canonical declarada y elegida por Google. La auditoria funcional encontro que la descarga podia destruir silenciosamente la cuadricula del QR: un contenido de 1.200 caracteres genera 181 modulos, pero los tamanos seleccionables de 100 y 200 px no permiten asignar ni un pixel por modulo; incluso 600 px introducia limites fraccionarios. Los archivos se descargaban sin error pese a no poder decodificarse.

- La exportacion lee el numero real de modulos desde el `viewBox` del SVG.
- El PNG usa al menos cuatro pixeles enteros por modulo y ajusta automaticamente la dimension final al multiplo seguro mas cercano.
- El lienzo desactiva la interpolacion para conservar bordes nitidos.
- La interfaz presenta el control como tamano minimo e informa de la dimension final y los pixeles por modulo antes de descargar.

La prueba real local y de produccion uso 1.200 caracteres ASCII con un minimo solicitado de 100 px: el resultado fue un PNG de `724 x 724`, cuatro pixeles por cada uno de los 181 modulos, y `jsQR` recupero exactamente los 1.200 caracteres. ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `ca3c89c` y `7a5ac72`, produccion mantiene canonical propio, `index, follow`, JSON-LD valido, 390 px de documento en un viewport de 390 px y consola limpia. No se cargan hosts externos, Moneytag, popunders ni redes publicitarias. El sitemap publica `2026-08-21T09:02:34.000Z` como `lastmod`. IndexNow recibio solo `/generador-qr` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Extractor de colores: paleta sin sesgo de muestreo

GSC muestra `/extractor-colores` como `Descubierta: actualmente sin indexar`, asociada al sitemap y con enlaces referentes desde la portada y la calculadora de sueldo neto. La auditoria funcional encontro que la paleta analizaba uno de cada ocho pixeles mediante un salto lineal. En imagenes cuyo ancho era multiplo de ocho, el algoritmo visitaba siempre las mismas columnas y podia omitir por completo el color dominante.

El caso de regresion usa una imagen `8 x 8` con una columna roja y siete azules. Antes de la correccion, la herramienta devolvia solo `#FF0000`, aunque el rojo ocupaba 8 pixeles y el azul 56. El extractor analiza ahora todos los pixeles opacos del lienzo ya limitado a un maximo de `500 x 500`, por lo que elimina el sesgo con un techo de 250.000 iteraciones.

En local y produccion, la misma imagen devuelve `#0000FF` como primer color y conserva `#FF0000` como secundario. ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `af57ef1` y `1977a87`, produccion mantiene canonical propio, `index, follow`, JSON-LD valido, 390 px de documento en un viewport de 390 px y consola limpia. No se cargan hosts externos, Moneytag, popunders ni redes publicitarias. El sitemap publica `2026-08-21T09:10:08.000Z` como `lastmod`. IndexNow recibio solo `/extractor-colores` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

Una segunda revision corrigio la copia de los codigos HEX. La interfaz espera ahora la resolucion de Clipboard API antes de mostrar el icono y anunciar el exito; si la API no existe o rechaza el permiso, muestra una instruccion para copiar el codigo manualmente. Los temporizadores de confirmaciones anteriores se cancelan para que no borren un estado mas reciente. En local y produccion, un `NotAllowedError` simulado mostro el error para `#F0F0FF` sin anunciar exito; el camino correcto escribio exactamente `#0064A0`, retiro el error y anuncio la copia. Tras desplegar `52afb93` y `f6762af`, el sitemap publica `2026-08-21T09:33:30.000Z` como `lastmod`; IndexNow respondio `HTTP 200` y GSC acepto el sitemap con `HTTP 204`. La inspeccion posterior indica `Google no reconoce esta URL`, un estado de descubrimiento que no altera la indexabilidad tecnica verificada.

## Temporizador: silencio aplicado durante la alarma activa

GSC muestra `/temporizador` como `Google no reconoce esta URL`, sin rastreo, canonical elegida, referentes ni sitemap asociado todavia. La auditoria funcional encontro que los pitidos programados conservaban el valor de silencio existente al iniciar la alarma. Por ello, silenciar una alarma activa no detenia los siguientes pitidos y reactivar el sonido durante una alarma iniciada en silencio tampoco los habilitaba.

- Una referencia sincronizada conserva el estado de silencio actual para las llamadas diferidas de la alarma.
- El boton actualiza de forma atomica la referencia usada por el audio y el estado usado por la interfaz.
- El comportamiento visual y el audio permanecen sincronizados al cambiar el silencio durante una alarma activa.

Las pruebas local y de produccion instrumentaron `AudioContext` con un temporizador de un segundo. Al silenciar tras el primer pitido, el contador permanecio en `1`; al iniciar silenciado y reactivar el sonido durante la alarma, paso de `0` a `2`. ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `79cfbaa` y `462e8b4`, produccion mantiene canonical propio, `index, follow`, JSON-LD valido, 390 px de documento en un viewport de 390 px y consola limpia. No se cargan hosts externos, Moneytag, popunders ni redes publicitarias. El sitemap publica `2026-08-21T09:19:29.000Z` como `lastmod`. IndexNow recibio solo `/temporizador` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`; estas respuestas notifican el cambio, pero no garantizan su indexacion.

## Regla de tres: resultados extremos sin desbordamiento intermedio

GSC muestra `/calculadora-regla-de-tres` como `Descubierta: actualmente sin indexar`, asociada al sitemap y con `/calculadora-dias` como URL referente. La auditoria funcional encontro que la formula multiplicaba antes de dividir. Aunque el resultado matematico fuera representable, un producto intermedio podia subdesbordar a cero o desbordar a infinito.

- El calculo evalua las tres ordenaciones algebraicamente equivalentes y prioriza un resultado finito no nulo.
- Se conserva primero el orden original cuando ya es seguro, para no alterar innecesariamente sus redondeos.
- Los valores menores que `1e-6` o mayores o iguales a `1e12` se presentan en notacion cientifica para evitar cadenas de cientos de caracteres.

Las pruebas local y de produccion confirmaron `1E-200` tanto en regla directa como inversa para tres entradas `1e-200`. El caso opuesto con `A = 1e-308`, `B = 1e308` y `C = 1e-308` devolvio `1E308`, por lo que la correccion no intercambia subdesbordamiento por desbordamiento. ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. Tras desplegar `8386970` y `e94af90`, produccion mantiene canonical propio, `index, follow`, JSON-LD valido, 390 px de documento en un viewport de 390 px y consola limpia. No se cargan hosts externos, Moneytag, popunders ni redes publicitarias. El sitemap publica `2026-08-21T09:27:34.000Z` como `lastmod`. IndexNow recibio solo `/calculadora-regla-de-tres` con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Consolidacion de variantes pSEO y controles editoriales

La consulta directa del inventario historico encontro 43 variantes pSEO retiradas: 17 de sueldo neto, 14 de IVA y 12 de prestamos. Sus rutas devolvian `404`; no aportaban demanda diferenciada ni contenido suficiente para reactivarlas, pero podian conservar enlaces o señales historicas.

- Las 43 rutas exactas redirigen ahora con `308` a `/calculadora-sueldo-neto`, `/calculadora-iva` o `/calculadora-prestamos` segun su grupo.
- No se introdujeron comodines que pudieran capturar slugs desconocidos y la ruta dinamica pSEO permanece desactivada.
- Ninguna variante retirada aparece en el sitemap.
- El panel privado `/revision-seo` conserva el archivo historico y el borrado, pero ya no ofrece generacion manual, variantes ni configuracion de frecuencia automatica.
- La auditoria editorial bloquea articulos por debajo de 500 palabras, ademas de relaciones invalidas, falta de fuentes o ausencia de enlace a la herramienta principal.

La prueba local y de produccion confirmo `308` y destino correcto para `/calculadora-sueldo-neto-madrid`, `/calculadora-iva-autonomos` y `/calculadora-prestamos-coche`. El panel sigue protegido con `401` y autenticacion basica. La inspeccion GSC de esas tres variantes devuelve `Google no reconoce esta URL`; la redireccion queda preparada para consolidar cualquier señal futura, no como garantia de indexacion.

Tambien se corrigio el traductor de codigos, que convertia entradas binarias o hexadecimales invalidas en caracteres de control. La salida se deriva directamente de la entrada, valida el formato y muestra un error accesible. La copia espera la confirmacion real del portapapeles y ofrece una alternativa manual si el navegador la rechaza. En produccion, `HI` como binario invalido muestra el error; `01001000 01001001` devuelve `HI`, y la copia confirmada escribe exactamente `HI`, el resultado visible. El traductor y la ruleta conservan su clasificacion `noindex,follow` y permanecen fuera del sitemap y de IndexNow.

Se completaron las Twitter Cards especificas de calculadora de dias, regla de tres, extractor de colores, enlace de WhatsApp, firmas de email y temporizador. Produccion expone `summary_large_image`, titulo propio e imagen social en las seis rutas.

ESLint completo, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. La auditoria registro 10 articulos, todos con al menos 500 palabras, fuentes y enlace contextual a su herramienta. Los cambios se publicaron en los commits `65c04d5`, `4debe5d`, `6a9ee79`, `2f8eaac`, `7dfaaaa`, `6668411` y `82d1d1f`. IndexNow recibio las nueve URLs canonicas indexables afectadas con `HTTP 200`; GSC acepto el sitemap con `HTTP 204`.

## Simbolos indexables y carga QR accesible

La revision del inventario detecto que `/simbolos-copiar` seguia clasificada globalmente como herramienta de bajo valor pese a ofrecer una intencion propia, contenido editorial y una biblioteca interactiva amplia sin URL competidora. La pagina se retiro de esa clasificacion y se preparo como destino indexable sin crear variantes ni articulos canibales.

- La metadata sustituye la afirmacion inexacta de miles de caracteres por una descripcion verificable de mas de 200 simbolos Unicode.
- La biblioteca contiene 226 simbolos unicos organizados en seis categorias, con nombre accesible y foco visible en cada boton.
- La copia espera la confirmacion real de Clipboard API, usa un mecanismo alternativo y muestra un error accesible si ambos fallan.
- Canonical, Open Graph, Twitter Card, JSON-LD, `index, follow`, enlaces internos y `lastmod` quedaron alineados con el contenido visible.
- El texto explica que la apariencia y compatibilidad de Unicode pueden variar entre fuentes, aplicaciones y dispositivos.

La misma revision encontro que el input de imagen de `/lector-qr` usaba `display: none`, por lo que no podia recibir foco de teclado. Ahora permanece visualmente oculto con `sr-only`, esta asociado explicitamente a su etiqueta y comunica la activacion mediante Enter. En produccion, una imagen de prueba se proceso en el navegador y devolvio exactamente `https://example.com/ruta?utm_source=qr-test`.

ESLint completo y dirigido, TypeScript, auditoria editorial, `git diff --check` y dos builds de 58 rutas finalizaron correctamente. Lighthouse movil sobre simbolos obtuvo 100 en accesibilidad, buenas practicas y SEO, sin fallos; el documento mide 390 px en un viewport de 390 px. Produccion sirve el commit `7f24379`, 226 botones sin duplicados, canonical propio y `index, follow`. El sitemap publica `2026-08-21T10:12:56.000Z` para simbolos y lector QR.

IndexNow recibio solo `/simbolos-copiar` y `/lector-qr` con `HTTP 200`, y GSC acepto el sitemap con `HTTP 204`. La inspeccion de GSC mantiene `/lector-qr` como `Enviada e indexada`, con rastreo correcto y canonical coincidente; `/simbolos-copiar` todavia figura como `Google no reconoce esta URL`, por lo que el despliegue elimina barreras tecnicas pero no garantiza seleccion inmediata para el indice. No se publico un articulo nuevo porque GSC no muestra demanda diferenciada suficiente. Los recursos de produccion proceden exclusivamente de `cajautil.com`; no se cargan Moneytag, popunders ni redes publicitarias.

## Refuerzo interno del conversor de unidades

El rastreo posterior confirmo 43 URLs con respuesta `200`, 43 indexables, ninguna incidencia y ninguna duplicidad de titulo o descripcion. No habia paginas huerfanas, pero `/conversor-unidades` solo recibia un enlace interno de contenido. GSC la mostraba como `Descubierta: actualmente sin indexar`, asociada al sitemap y con la portada como unica pagina referente conocida.

Se añadieron dos enlaces editoriales contextuales, no bloques genericos: la regla de tres recomienda unificar magnitudes antes de calcular una proporcion y la calculadora de calorias indica convertir libras, pies o pulgadas a las unidades metricas exigidas por Mifflin-St Jeor. Tras desplegar `a0adcca`, el rastreo publico retiro `/conversor-unidades` del grupo de URLs con menos de tres paginas fuente; las dos referencias aparecen en el HTML inicial.

ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. Produccion mantiene canonical propio, `index, follow`, cero desbordamiento movil y consola limpia en ambas paginas fuente. El sitemap publica `2026-08-21T10:33:42.000Z` para `/calculadora-regla-de-tres` y `/calculadora-calorias`. IndexNow recibio solo esas dos URLs modificadas con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`. No se publico contenido nuevo: GA4 sigue sin sesiones organicas en los ultimos siete dias completos y GSC no muestra una intencion diferenciada que justifique otra URL sin canibalizacion. Los recursos comprobados proceden exclusivamente de `cajautil.com`, sin Moneytag, popunders ni redes publicitarias.

## Cierre de rutas internas debiles en herramientas

El mismo rastreo identificaba otras tres herramientas indexables con solo dos paginas fuente: regla de tres, validador IBAN y firmas de email. Se añadieron enlaces editoriales donde completan una tarea real: porcentajes deriva relaciones directas o inversas a regla de tres; IVA recomienda validar la estructura del IBAN incluido en una factura sin prometer titularidad; y el generador de WhatsApp propone incorporar el enlace en una firma profesional.

Tras desplegar `d12ac8c`, las tres referencias aparecen en el HTML inicial y ninguna herramienta queda en el grupo con menos de tres enlaces internos. La unica URL restante en ese grupo es `/aviso-legal`, una pagina institucional que ya esta enlazada desde la navegacion legal y no necesita refuerzo comercial artificial. Un nuevo rastreo mantiene 43/43 URLs con `200`, 43 indexables, cero incidencias y cero duplicados de titulo o descripcion.

ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. La comprobacion movil mantiene canonical, `index, follow`, cero desbordamiento y consola limpia; los recursos cargados pertenecen exclusivamente a `cajautil.com`. El sitemap publica `2026-08-21T10:39:14.000Z` para porcentajes, IVA y enlace de WhatsApp. IndexNow recibio esas tres URLs modificadas con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Entidades de autor y organizacion coherentes

La auditoria E-E-A-T encontro que la metadata global atribuía la autoria a la marca, mientras los articulos mostraban la revision de Raul Santos. Ademas, el `Organization` global usaba el GitHub personal del autor como `sameAs` y las paginas Sobre nosotros y Contacto declaraban organizaciones independientes sin un identificador compartido.

El grafo estructurado usa ahora entidades estables: `WebSite` referencia como publisher a `https://cajautil.com/#organization`; la organizacion identifica fundador y contacto; `Person` conserva el GitHub personal y su relacion `worksFor`; AboutPage, ContactPage y Article enlazan esos mismos `@id`. La metadata visible declara a Raul Santos como autor y mantiene CajaUtil.com como publisher. No se añadieron titulaciones, certificaciones ni datos legales que no puedan demostrarse.

ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. En produccion se verificaron autor, publisher, founder, `worksFor`, canonical, `index, follow`, fechas de articulo, cero desbordamiento movil y consola limpia. El rastreo conserva 43/43 URLs con `200` e indexables, sin incidencias ni metadata duplicada. El sitemap publica `2026-08-21T10:46:33.000Z` para portada, Sobre nosotros y Contacto. IndexNow recibio esas tres URLs con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Firmas de email: carga de logo accesible y limitada

GSC muestra `/generador-firmas-email` como `Google no reconoce esta URL`, sin rastreo, canonical elegida, referentes ni sitemap reconocido todavia. La auditoria funcional encontro que el selector de logo usaba `display: none`, quedaba fuera del orden de teclado y aceptaba cualquier tipo y tamano de imagen antes de convertirla completa a una URL de datos.

- El input permanece visualmente oculto con `sr-only`, recibe foco de teclado y muestra un anillo visible en su etiqueta.
- La carga acepta exclusivamente PNG, JPG y WebP de hasta 2 MB antes de usar `FileReader`.
- Los errores y el nombre del archivo seleccionado se anuncian mediante regiones vivas accesibles.
- Las etiquetas del formulario y el titulo de la vista previa pasaron de `text-slate-400` a `text-slate-600` para cumplir contraste WCAG AA.

Las pruebas locales rechazaron un PNG de 11,5 MB y un archivo JSON con mensajes accesibles, aceptaron un PNG de 436 KB, mostraron su vista previa y copiaron la firma. El selector entra en foco con `display: block`, su etiqueta coincide con `:focus-within` y no existe desbordamiento horizontal. Lighthouse movil paso de 96 a 100 en accesibilidad y obtuvo 100 en buenas practicas y SEO, sin fallos. ESLint dirigido, TypeScript, auditoria editorial, `git diff --check` y el build de 58 rutas finalizaron correctamente. El cambio se publico en Git como `9b6e4cf`, pero Vercel rechazo el despliegue con `Deployment rate limited — retry in 24 hours`; produccion conserva la version anterior. Por ello no se enviaron todavia esta URL ni el nuevo `lastmod` a IndexNow o GSC: hacerlo antes del despliegue notificaria contenido que aun no esta disponible.

## Precision Unicode, coherencia numerica y accesibilidad

Una revision adversarial adicional confirmo siete defectos en herramientas existentes. Se corrigieron sin crear nuevas URLs ni ampliar contenido editorial sin demanda:

- El contador de palabras mide caracteres por grafemas Unicode, por lo que un emoji compuesto o una letra con marca combinada cuentan como una unidad visible.
- El selector de imagen del compresor WebP permanece accesible por teclado mediante `sr-only`, foco visible y ayuda asociada.
- Interes compuesto, prestamos y edad usan contrastes WCAG AA y una jerarquia de encabezados secuencial en sus resultados.
- La metadata y el JSON-LD de la calculadora de dias distinguen la diferencia exacta en dias de las equivalencias aproximadas en meses y anos.
- La calculadora de IMC redondea primero a una decimal y usa ese mismo valor visible para clasificar el rango, evitando resultados como `25,0` descritos como rango de referencia.

Las pruebas locales confirmaron que `👨‍👩‍👧‍👦 é` contiene tres grafemas y dos sin espacios, que un WebP real puede seleccionarse por teclado y convertirse, y que un IMC bruto de `24,9975` se muestra como `25,0` y se clasifica como sobrepeso. Lighthouse movil obtuvo 100 en accesibilidad, buenas practicas y SEO en contador de palabras, compresor WebP, IMC, interes compuesto, prestamos y edad. ESLint dirigido, TypeScript, `git diff --check` y el build de produccion de 58 rutas finalizaron correctamente.

Vercel completo posteriormente el despliegue `d109976`, que tambien incorporo la carga de logo de firmas pendiente. El rastreo publico confirmo 43/43 URLs con respuesta `200`, indexables, sin incidencias ni metadata duplicada. Produccion reprodujo los casos de grafemas e IMC y convirtio `og-image.png` de 545,3 KB a 37,3 KB; la herramienta WebP solo cargo recursos de `cajautil.com`. IndexNow recibio las siete URLs modificadas con `HTTP 200` y GSC acepto el sitemap con `HTTP 204`.

## Evidencia original y reproducible

La inspeccion de GSC mantiene `/sobre-nosotros` y las guias de QR y WebP como descubiertas pero todavia sin indexar. Los datos de rendimiento tampoco muestran una intencion nueva que justifique otra URL. Para reforzar experiencia demostrable sin publicar contenido canibal, se incorporaron resultados reales de pruebas ejecutadas sobre las herramientas existentes:

- `/sobre-nosotros` explica el proceso de control y muestra cuatro casos concretos de QR, WebP, prestamos e IMC con entradas y resultados comprobados.
- La guia QR documenta una prueba de 1.200 caracteres, 181 modulos, cuatro pixeles por modulo, salida de 724 x 724 px y lectura posterior exacta con jsQR.
- La guia WebP documenta la conversion de `og-image.png`, 640 x 640 px y 545,3 KB, a un WebP de 37,3 KB con calidad 80; tambien limita expresamente el resultado a ese archivo.
- Las dos pruebas incluyen instrucciones para repetir el flujo con las herramientas publicas y distinguen un caso observado de una garantia general.

La auditoria editorial registra ahora 1.250 palabras en la guia QR y 989 en WebP; los diez articulos mantienen al menos 500 palabras, fuentes externas y enlace a su herramienta. ESLint dirigido, TypeScript, `git diff --check` y el build de 58 rutas finalizaron correctamente. La vista movil de `/sobre-nosotros` no desborda, su `AboutPage` incluye `dateModified` y Lighthouse obtuvo 100 en accesibilidad, buenas practicas y SEO.

## Texto estructurado de articulos sin sintaxis Markdown

La representacion visible de las tablas era correcta, pero `articleBody` conservaba tuberias y separadores Markdown en siete de los diez objetos `Article`. El problema afectaba exclusivamente al texto enviado en JSON-LD: por ejemplo, una tabla aparecia como `| Campo | Valor | |---|---|` aunque el usuario viera celdas normales.

El sanitizador comun elimina ahora las filas de alineacion, separa las celdas con puntuacion legible y retira solo marcadores de lista al inicio de linea. Los guiones de rangos y nombres, y los guiones bajos de archivos como `og-image_q80.webp`, se conservan. La comprobacion de los diez articulos devuelve cero tuberias y cero separadores de tabla en `articleBody`; las tablas visibles, canonical, fechas y contenido no cambian.
